# 2026-09 update log

Index and query commands: [README.md](../../README.md). New entries go at the end.

---

## U-20260922-01 · 2026-09-22 · Adopt progress/architecture/docs-updates rules · #docs #migration

- **What**: `CLAUDE.md` gained the workspace rule section (commit at every stage; `Progress.md` holds open items only; finished work is recorded in `docs/updates/`; `architecture.md` is the short overview). Added `architecture.md`. Moved all of `WHATS_NEW.md` into monthly batches here (one `#release` entry per former section; `WHATS_NEW.md` now points here) and moved finished history out of `Progress.md` (U-20260819-02, U-20260822-01, U-20260824-02).
- **Files**: `CLAUDE.md`, `architecture.md`, `Progress.md`, `WHATS_NEW.md`, `docs/updates/`, README links.
- **Open items**: see `Progress.md`.

## U-20260922-02 · 2026-09-22 · Point project URLs at Integration-Automation/AutoControlGUI · #done #metadata

- **What**: `pyproject.toml`, `dev.toml`, the three READMEs (clone command and homepage) and the `webrunner_bridge` module docstring pointed at `Intergration-Automation-Testing/AutoControl` (and `…/WebRunner`); they now name `Integration-Automation/AutoControlGUI` and `Integration-Automation/WebRunner`. Workspace item X-11.
- **Not changed**: `je_auto_control/utils/mcp_registry/registry.py` keeps `_SERVER_NAME` and `_REPO_URL`, because the `io.github.<owner>/…` name is a registry identity; see the new `DECIDE` section in `Progress.md`.
- **How**: committed on `fix/project-urls`, branched from `feat/coverage-to-80` in a separate worktree, so the main working tree (loaded live by Jeffrey_RPA as an editable install) was not touched.
- **Files**: `README.md`, `README/README_zh-CN.md`, `README/README_zh-TW.md`, `dev.toml`, `je_auto_control/utils/webrunner_bridge/__init__.py`, `pyproject.toml`, `Progress.md`.
- **Open items**: `Progress.md` › MCP registry 的名稱還是拼錯的舊 org.
