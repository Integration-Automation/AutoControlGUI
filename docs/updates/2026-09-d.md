# 2026-09 update log (d)

Index and query commands: [README.md](README.md). New entries go at the end.

---

## U-20260925-32 · 2026-09-25 · Two new audit tests failed CI: the window probe needs qt_material, which the headless jobs lack, and 3.14 on Linux and macOS parses JSON nested 50,000 deep · #testing #ci

- **Found by**: PR #489's `pytest-headless` jobs failed on 800898c2 and 4745ff8d, on every platform. Locally everything had passed.
- **`test_gui_tab_lifecycle_audit.py::test_destroying_the_window_removes_its_listeners`** (all jobs):
  - The probe imports the real main window, which styles itself with `qt_material`. The headless jobs do not install it (`ModuleNotFoundError: No module named 'qt_material'`).
  - The test now skips without it, as the other main-window tests already do.
- **`test_server_report_audit.py`, four tests** (Python 3.14 on Ubuntu and macOS):
  - On those builds the C JSON parser read all 50,000 levels of `[[[...]]]` without a `RecursionError`. The payload then parsed as a list, so the tests saw "body must be an object" (REST), `-32600` (MCP) or a parsed value (webhook, clients) instead of the handling under test.
  - How deep the parser goes first depends on the build and the stack. The tests now simulate it: a `too_deep` fixture makes `json.loads` raise `RecursionError` for that payload. What they check, that each server and client handles the error, is unchanged.
- **Files**: `test/unit_test/headless/{test_gui_tab_lifecycle_audit,test_server_report_audit}.py`.

## U-20260925-33 · 2026-09-25 · Integration helpers at their edges: secrets nested in arguments are masked, credentials stay behind on redirects, assert_http and config sync go through http_client, Office / S3 / SQLite errors stay in the framework family, the IMAP trigger fires once per message and names any mailbox, cassettes keep no credentials · #bugfix #security #integration

These findings come from an audit of the database, HTTP, notification, Office, S3, data-source and IMAP helpers, all reproduced with loopback servers and fakes. `test_integration_helpers_audit.py` (new, 19) fails 16 of its 18 runnable tests on the old code; the other 2 are guards, and the Office test skips without openpyxl.

- **Secrets nested in arguments**, high:
  - Masking by name applied to top-level arguments only. `{"smtp": {"password": ...}}` and `{"headers": {"Authorization": ...}}` reached the executor log and the run record's keys in plain text.
  - `redact_actions` now masks sensitive names at every depth. The credential headers (`Cookie`, `X-Api-Key`...) are among those names.
  - `AC_notify_webhook`'s `url`, a Slack, Discord or Teams key in itself, is masked too.
- **One list of credential headers**: `http_headers.CREDENTIAL_HEADERS`. `http_client` strips all of them on a redirect to another origin; before, `X-Api-Key` and `X-Auth-Token` were forwarded. The cassette uses the same list.
- **`http_client`**:
  - A 4xx/5xx body cut off mid-chunk raised `http.client.IncompleteRead`, which is no `OSError`, from inside the `except HTTPError` clause, and aborted the script. The error response is now read and closed in its own handler, and a malformed one becomes `URLError`.
  - A reply nested thousands deep raised `RecursionError` out of every `http_request`; `_try_json` now treats it as not JSON.
- **`assert_http`**: it called `urlopen` directly, bypassing the egress policy (a locked-down run still reached `/internal`), the redirect checks and the 64 MiB body cap. It now uses `build_call` / `perform_call`.
- **Config sync**: plain `urlopen` followed a redirect to any host with `X-Signaling-Secret` attached, and ignored the egress policy. It now goes through `http_client` without following redirects; a 3xx is a `ConfigSyncError`.
- **Office readers** (`read_workbook`, `read_document`, `read_presentation`): a file that is not the format raised `zipfile.BadZipFile` or python-docx's `PackageNotFoundError` and aborted the script. These are now `AutoControlActionException`.
- **S3 store**: botocore's `ClientError` escaped all four `AC_s3_*` commands. It is now `ArtifactStoreError(AutoControlException, RuntimeError)`.
- **SQLite data source**: `connect` sat outside the error handling, so a file that cannot be opened raised `sqlite3.OperationalError` past `AC_for_each_row`.
- **IMAP trigger**:
  - Two polls running together (the watcher and `AC_email_trigger_poll_once`) both fired the same message. A UID is now claimed atomically before it is fetched.
  - A UIDVALIDITY change now clears the seen UIDs, so a new message reusing an old UID is no longer skipped forever.
  - Mailbox names are sent in modified UTF-7 (RFC 3501 5.1.3). `已處理` raised `UnicodeEncodeError`, and `R&D` went out unencoded. Names already in that form are left alone.
- **Cassettes**: credential query parameters, JSON body members and response members are now masked, not only headers. Replay compares the masked forms, so recorded calls still match.
- **Tests**:
  - Three test helpers were updated: the `assert_http` tests patch `http_client`'s transport rather than `urlopen`, and three fake IMAP clients answer `response()` as imaplib does.
  - The 762 related tests pass.
- **Files**:
  - Code: `utils/{http_headers,executor/action_redaction,http_client/http_client,http_cassette/http_cassette,assertion/assertions,config_sync/client,office/office,artifact_store/s3_store,data_source/data_source,triggers/email_trigger}.py`.
  - Also `CHANGELOG.md` and `architecture_explore.md` (line counts).

## U-20260925-34 · 2026-09-25 · Window and OS helpers at their edges: file dialogs need the exact title and the front before typing, watchdog keys go to the popup, psutil and regex errors are framework errors, empty needles are refused, file drops use absolute paths, the Window Manager acts on the selected window; input paths Jeffrey_RPA runs are recorded, not changed · #bugfix #windows #gui

These findings come from an audit of window management and OS integration: 16 bugs, each reproduced with probes that faked every Win32 call that would act. `test_window_system_audit.py` (new, 13) fails 10 of its 12 runnable tests on the old code; one is a guard, and one needs Windows.

- **Held back while Jeffrey_RPA runs**: its webrunner batch and Discord bot were running (`webrunner.pid` alive), and the bot calls `post_key_to_window`, `focus_window` and `save`/`restore_window_layout`. Following the workspace rule for code a live batch runs, the fixes to those paths are recorded under `Progress.md`'s blocked keyboard/mouse item, with locations and fixes:
  - `post_key` typing a character three times;
  - focus, show and z-order calls reporting success when Windows refuses;
  - cloaked and zero-area windows being listed;
  - saved layouts drifting 7 px and shrinking on every restore, and tiles running under the taskbar;
  - `wait_for_window` sleeping past its timeout.

  macOS minimized windows that cannot be restored need a macOS run and got their own item.
- **File dialogs**, high:
  - The default driver matched "Open" / "Save As" as a substring and never focused the window. "How to reopen closed tabs - Google Chrome" was taken for the Open dialog, and the path and Enter were typed into whatever window was active.
  - It now waits for a window with exactly that title and brings it to the front (the new `WindowManageBackend.bring_to_front`, which confirms the window really became the foreground). Otherwise nothing is typed and the result is `handled: False`.
- **Watchdog key rules**, high: the key was pressed in the user's active window. The popup is now brought to the front first; if Windows refuses, the rule records an error instead. An infinite poll interval, which raised `OverflowError` in the guard thread, is now clamped.
- **Errors outside the family**, high:
  - MCP `ac_kill_process` let `psutil.AccessDenied` escape; it ended the stdio worker without a reply. It is now `AutoControlActionException`.
  - `wait_until_window_title` let `re.error` escape for a bad pattern; it is now `ValueError`.
- **Empty needles**: `""` is in every name, so `wait_until_process("")`, `wait_until_window_title("")` and `assert_process("")` succeeded at once. They are now refused.
- **File associations**: an unregistered extension answered with the Open With picker (`OpenWith.exe`); with `ASSOCF_INIT_IGNOREUNKNOWN` it is `None`, as documented.
- **File drops**: relative paths went into `CF_HDROP` as given, and the receiving program resolved them against its own working directory. Clipboard file lists and window drops now carry absolute paths.
- **Window Manager tab**: Focus and Close acted on the first window whose title contained the selected row's title, so closing `notes.txt - Notepad` closed the unsaved `*notes.txt - Notepad`. They now act on the row's window id. Its filter refresh was a single-shot timer with no context, so it fired after the tab was deleted and read the deleted filter box. It is now tied to the tab; the WebRTC panel's five single-shots had the same shape and are tied to it too.
- **Tests**: two tests pinned the old behaviour and were updated: the watchdog key test (now fronts the popup) and the Window Manager error test (acts by id). The 1,300 related tests pass, and the WebRTC panel's 292.
- **Files**:
  - Code: `wrapper/window_backends/base.py`, `utils/{file_dialog/file_dialog,watchdog/popup_watchdog,mcp_server/tools/_handlers_system,smart_waits/waits,assertion/assertions,file_assoc/file_assoc,clipboard_files/clipboard_files,file_drop/file_drop}.py`, `gui/window_tab.py`, `gui/remote_desktop/webrtc_panel.py`.
  - Docs: the v8/v9 feature docs (Eng/Zh), `Progress.md`, `CHANGELOG.md`, and `architecture_explore.md` (line counts).

## U-20260925-35 · 2026-09-25 · SonarCloud's reliability gate failed on the OpenAI backend's first-choice read; it now takes the choice without indexing · #ci #agent

- **Found by**: PR #489's SonarCloud check failed from cb06a043 on, with `new_reliability_rating` 4 against a threshold of 1. The API (`issues/search?types=BUG&pullRequest=489`) listed one issue: `pythonbugs:S6466` at `agent/backends/openai.py:128`, "Fix this access on a collection that may trigger an 'IndexError'".
- **Cause**: `choices[0]` sat one line below an `if not choices: raise`. Sonar did not carry that guard through the `list(... or [])` above it, so the check was a false positive.
- **Change**: `choice = next(iter(response.choices or ()), None)` followed by a `None` check. The behaviour is the same, the no-choices case is still `AgentBackendError`, and there is no suppression marker.
- **Files**: `utils/agent/backends/openai.py`. `test_agent_vlm_audit.py` and `test_llm_agent_audit.py` pass (36).

## U-20260925-36 · 2026-09-25 · A sweep of less-visited utils subpackages: three locators answer in screen coordinates, the voice router is thread-safe, failure signatures ignore unit-glued numbers, delta counts match their summary, no home directory is survivable, infinite samples and clipped checkboxes, dominant contrast colours, opaque URL schemes · #bugfix #vision #audit

These findings come from a sweep of the 58 `utils/` subpackages that no audit test imported; about 50 were read closely and 17 bugs confirmed with probes. `test_utils_sweep_audit.py` (new, 20) fails 19/20 on the old code; the 20th guards drive-letter paths.

- **Region-local coordinates**, high: `match_theme`, `propose_elements` and `read_barcodes` grabbed the screen through `_haystack_gray` / `_grab_gray`, which drop the capture's origin. So they answered in coordinates relative to the region: `match_theme` said (55, 35) for a target every other matcher places at (555, 335). All three now take the origin with the frame and move their results by it.
- **`match_ensemble`**: each template took its own screenshot, so on the animated targets the module exists for, the votes were cast on different frames. It now grabs once and matches every template against that frame.
- **Voice router**: `default_voice_router` is shared by the executor and the MCP handlers. `match` scored one snapshot of the commands and indexed another, so a concurrent `register` ran the wrong command (9,717 of 20,000 dispatches in a probe), and `clear` raised `IndexError`. It now holds a lock and matches on a single snapshot.
- **Failure signatures**: `\b\d+\b` does not match a number glued to its unit (`5s`, `1532ms`), so one timeout got a new signature on every run, and five identical failures fell into four groups. Such numbers are now normalised too; the address marker `0x<addr>` is left alone.
- **Observation deltas**:
  - `AC_delta_observation` counted added, removed and changed on the raw element lists, while the summary used the flattened, interactive-only, viewport-clipped frames. It said `added: 0` beside a `+` line and `changed: 1` for a label the summary omitted. The new `observation_delta_index` feeds both.
  - A removed element was printed with its index from the previous frame, which now names a different element. `-` lines no longer carry an index.
- **No home directory** (a service account, a scrubbed environment):
  - `import je_auto_control` raised `RuntimeError` from the log handler's default path. That path now falls back to the temp directory.
  - `validate_path` raised a builtin `RuntimeError` past its callers. The home root is skipped, and `~` becomes `PathNotAllowedError`.
- **Infinite durations**: `percentile` interpolated `inf + f*(inf - inf)` into NaN, and `max(min_s, nan)` returned the floor. So two infinite samples gave the *shortest* timeout. The percentile no longer interpolates between equal values, and a NaN clamps to `max_s`.
- **Checkboxes**: a box partly off the frame (negative x or y) wrapped around or emptied the slice, so a checked box read `unchecked`. The box is now clamped to the frame.
- **Contrast**: the "dominant" colours were group means, pulled toward the other side by anti-aliased edge pixels. #767676 on white (4.54:1) read 3.23:1, a false AA failure. Each group now uses its most frequent colour.
- **Smaller contracts**:
  - `plan_open` let opaque schemes other than `mailto`/`tel` (`ms-settings:`, `javascript:`, `search-ms:`) through as file paths. They are now schemes, refused unless allow-listed.
  - A nameless element scored as if named "None".
  - The WebRunner bridge reported any `TypeError` from inside a command as "rejected params". The parameters are now checked with `inspect.signature(...).bind`, and the command's own errors pass through.
  - A mis-cased or unknown webhook transport (`"Discord"`) silently sent the raw payload. Transports are now case-insensitive, and unknown ones are refused.
  - `salient_regions`' docstring named a cut the code does not use.
- **Tests**: the 380 tests touching these modules pass, after one pinned normalisation was kept intact (`0x<addr>`).
- **Files**:
  - Code: `utils/{theme_normalize,element_proposal,barcode,match_ensemble,voice/voice_router,failure_signature,observation_delta,logging/logging_instance,path_guard,stats,adaptive_timeout,form_fields,contrast_map,shell_open,element_scoring,webrunner_bridge/bridge,notify_channels,saliency}` and the `AC_delta_observation` adapter in `executor/action_executor.py`.
  - Docs: the barcode feature doc (Eng/Zh), `CHANGELOG.md`, and `architecture_explore.md` (line counts).

## U-20260925-37 · 2026-09-25 · Remote desktop panels: Quick Connect verifies wss:// and forwards input, closing a session window no longer aborts the process, connect slots report every error, audio follows its box with the section collapsed, the share text quotes the running host, signaling and the USB browser go through http_client · #bugfix #remote-desktop #gui #security

This is the first half of an audit of the remote desktop GUI (`gui/remote_desktop/`); the `webrtc_panel.py` items and Quick Connect session ownership are still open. `test_rd_gui_audit.py` (new, 16) and `test_rd_data_audit.py` (new, 10) fail 23/26 on the old code. The 3 that pass are guards: the Quick Connect close path, a `file://` URL that does not exist, and an ordinary offer round trip.

- **`wss://` without TLS**, high: Quick Connect dialled `wss://` targets as plain `ws://`. The session went unencrypted to a host the operator took for TLS, and a real TLS host could not be reached. It now connects with a context that verifies the certificate. For a self-signed host, use the Advanced viewer with *Skip cert verification*.
- **Quick Connect input**: the popup wired only `closed` and `files_dropped`, so the remote screen ignored every click and key. Both viewers now wire the popup through one helper, `wire_remote_input`.
- **Abort on close**, high: the legacy viewer connected lambdas that held the panel. A closed window's deferred delete released the panel, and the panel's destructor deleted that window again, which aborted the process (a subprocess probe exits with code 3). This happened on the old code too. The helper now holds a bound-method sink weakly.
- **Connect errors**: both connect slots caught only `OSError`/`RuntimeError`. A host such as `a..b` fails IDNA encoding with `UnicodeError`, which escaped the slot, and the click went unanswered. `ValueError` and `AutoControlException` are now reported too.
- **Audio**: both panels read `isEnabled()` on the audio box. A collapsed Advanced section disables its children, so a ticked box was ignored once the section was closed. Both panels now keep an availability flag. A reconnect without Disconnect also left the old player running; it is now stopped.
- **Legacy viewer**:
  - The port box's minimum was 1, so it opened on port 1 and the "required" check never fired. 0 now means unset.
  - A session error left the window on its last frame and the audio running. It now tears the session down.
- **Host panel**:
  - The share text read the token, bind address and transport fields at copy time, so an edited field (or a host started elsewhere) was shared. It now quotes the settings the host started with, and names TLS/WSS when a certificate is set.
  - The preview decoded 4 frames a second while hidden. It now does nothing while hidden.
- **Stored and remote data**:
  - Before 1970, or a naive time, `timestamp()` / `astimezone()` raise `OSError` on Windows, which broke the address book's sort. Naive times are now read as UTC and conversion errors are caught.
  - The known-hosts table compared a naive `last_seen` with an aware `now` (`TypeError`).
  - Remote inbox rows that were not objects, or had a non-numeric or infinite size, raised mid-table. Such rows are now skipped or shown as sent.
  - The address book now normalises string or numeric tags and non-string times on load.
- **Signaling client**: it went straight to `urlopen`. That read `file://` URLs, raised `ValueError` for a URL without a scheme (past workers that catch only `SignalingError`), followed redirects carrying `X-Signaling-Secret`, and gave non-object replies to `.get("sdp")`. It now uses `http_client` without following redirects, as config sync does. Non-object replies and non-string SDP are `SignalingError`.
- **USB browser**: `fetch_remote_devices` sent its bearer token through `urlopen`, which carries it across a redirect to another host. It now uses `http_client`, which strips credentials there and applies the egress policy.
- **Target parser**: `[::1]:5555` kept its brackets, and `WSS://` was read as a host named `WSS`. Brackets are now stripped and schemes are case-insensitive.
- **Tests**: `test_remote_desktop_gui.py` now drives input through the window's signals (the `_send_mouse_*` helpers are gone), and shows the host panel for the preview test.
- **Files**:
  - Code: `gui/remote_desktop/{_helpers,connection_screen,viewer_panel,host_panel,webrtc_dialogs,webrtc_known_hosts,webrtc_panel}.py`, `gui/usb_browser_tab.py`, and `utils/remote_desktop/{signaling_client,address_book,connect_coordinator}.py`.
  - Docs: the Quick Connect feature doc (Eng/Zh), `CHANGELOG.md`, and `architecture_explore.md` (line counts).

## U-20260925-38 · 2026-09-25 · Codacy's gate on PR #489 had seven findings: subprocess rules on building a CompletedProcess or TimeoutExpired, a taskkill call reported one line below its marker, exec in a codegen test, a stdlib xml import that the test needs, and an unassigned QApplication · #ci #codacy

- **Found by**: the Codacy check on f437bcc0 reported `7 new issues (0 max.)` (read from the check run's annotations; the Codacy API token is still invalid). All seven were in code that landed in earlier batches.
- **Changes**:
  - `shell_exec._kill_tree`: the `taskkill` argv is built on its own line, so the call fits on one line with its `nosemgrep`. The marker used to sit on the line above, which the rule does not read.
  - `shell_exec.run_captured`: `subprocess.CompletedProcess(...)` builds the result and runs nothing. It now carries `nosemgrep`, and so do the two tests that raise `subprocess.TimeoutExpired`.
  - `test_codegen_cli_report_audit.py`: the generated test is written to `tmp_path` and imported with `importlib`, in place of `exec`.
  - `test_deprecation_hygiene.py`: the stdlib `xml` import the test exists to exercise now has `nosemgrep`.
  - `test_window_system_audit.py`: the `QApplication` is assigned.
- **Files**: `utils/shell_process/shell_exec.py`, five test files, and `architecture_explore.md` (line counts). The touched tests pass (65, 1 skipped).

## U-20260925-39 · 2026-09-25 · WebRTC panels and Quick Connect sessions: remote annotations are checked and bounded, the tray keeps the app alive only while hosting, a host stopped under the approval dialog or the stats thread is not an error, imports survive files of the wrong shape, Stop cancels a pending reconnect, a new window keeps the pen, empty recordings are not called saved, and Quick Connect treats ws:// sessions, errors and approval timeouts properly · #bugfix #remote-desktop #gui

This is the second half of the remote desktop GUI audit (the first is U-20260925-37). `test_rd_webrtc_audit.py` (new, 13) fails 13/13 on the old code. One design gap is left for the maintainer: the TCP and WS viewer slots in `registry` are shared by every panel. It is recorded in `Progress.md` (DECIDE).

- **Remote annotations**, high: the host panel ran `float()` / `int()` on the viewer's coordinates and width, so a string raised `ValueError` out of the slot. Strokes and points also had no limit, and every update repainted all of them, so a viewer could grow the host's memory and paint time without bound. `HostAnnotationOverlay.apply` now drops non-finite points, clamps the width to 1..32, keeps at most 200 strokes and 5,000 points per stroke.
- **Tray**: installing the tray icon switched off `quitOnLastWindowClosed` for good. Opening the WebRTC tab was enough, and closing the main window then left the process running with no host. `HostTrayIcon.set_hosting` now keeps the app alive only while a host runs.
- **Races with a stopped host**:
  - The approval dialog is modal, and Stop (or the tray's Stop) could run while it was open. The answer was then sent to `None` (`AttributeError`). The host is now re-read after the dialog closes.
  - The per-session stats thread read `self._multi_host` twice, and the GUI thread could clear it in between. It now reads it once.
- **Imports**: the trust-list and address-book imports raised `TypeError` for `{"viewers": 5}` and `UnicodeDecodeError` for a binary file, and deep nesting raised `RecursionError`; none of these was caught. `_read_import_entries` now reports every such file. The address book also accepted a numeric `host_id` (dropped on the next load) and a numeric MAC; it now imports strings only.
- **Viewer**:
  - The auto-reconnect was a `QTimer.singleShot` that Stop could not cancel, so it reconnected after Stop. It is now one timer that Stop stops.
  - With the pen on, a window created later started in input mode, so strokes reached the host as real clicks and drags. The new window now takes the pen state.
  - Stopping a recording that never received a frame said "Recording saved" for a file that was never created. It now says so (new `rd_webrtc_recording_empty` in the four catalogues; `SessionRecorder.has_output`).
- **Quick Connect**:
  - Closing a `ws://` popup left the session running, and the badge read only the TCP slot. Both transports now count.
  - A session error left the popup open on its last frame. The error now ends the session.
  - After the host's 60 s wait the connection was already denied, but the approval box stayed up. It now closes at the same timeout.
- **Size**: `webrtc_panel.py` shrank from 2,530 to 2,527 lines, and its row in the file-length list was lowered to match.
- **Files**:
  - Code: `gui/remote_desktop/{webrtc_panel,annotation_overlay,tray_icon,connection_screen,_helpers}.py`, `utils/remote_desktop/session_recorder.py` and the four language catalogues.
  - Tests: `test_rd_gui_audit.py` (a fake viewer gained `remote_host_id`).
  - Docs: `Progress.md`, `CHANGELOG.md` and `architecture_explore.md` (line counts).

## U-20260925-41 · 2026-09-25 · Script Builder: shown defaults match the executor (an edit no longer loosened an exact image match), required fields are required, positional arguments and wrapped files survive load and save, decimals can be typed under comma locales, odd RGB values load, and a new loop takes children · #bugfix #gui #script-builder

`test_script_builder_audit.py` (new, 10) and two new parity tests in `test_script_builder_schema_parity.py` fail 11/11 on the old code.

- **Shown defaults are written**, high: the form shows a field's schema default when the step has no value, and `_commit_field` writes every field on any edit. So selecting a hand-written `AC_locate_and_click` and changing its image wrote `detect_threshold: 0.8`, loosening the callable's exact 1.0 match: a click could land on a merely similar region. The same held for `AC_locate_image_center` and `AC_locate_all_image`. Their defaults are now 1.0. The `path` of `AC_agent_card`, `AC_generate_sbom` and `AC_mcp_manifest` is a placeholder now, not a default: the callables return the document when no path is given, and any edit used to write a file into the working directory.
- **Required fields**: `rows` / `cols` of `AC_grid_cells`, `AC_cell_for_point` and `AC_point_for_cell`, and `annotations` of `AC_annotate_screenshot`, were optional in the schema but required by the callable. A new step left them out and failed on its first run.
- **Guarding these**: `test_script_builder_schema_parity.py` now also checks that every non-empty schema default equals the callable's default, and that no optional field is required by its callable. Both checks walk the whole schema.
- **Silent data loss on save**:
  - `[command, [args]]` (positional arguments, which the executor accepts) lost its arguments on load, and anything past the second entry was dropped. Save then wrote `[command]` over the file. Positional arguments are now kept as `Step.args` and shown read-only in the form (new `sb_positional_args` in the four catalogues). Other shapes are refused.
  - A wrapped `{"auto_control": [...], ...}` file lost its other keys on save. They are now written back.
- **Comma locales**: `QDoubleValidator` validates in the default locale, which under French or German refuses `0.8` and accepts `0,8`, and `float()` rejects the latter. The form's numeric fields now validate in the C locale.
- **Smaller issues**:
  - A non-numeric RGB list raised `ValueError` out of the selection slot; it is now shown as written.
  - With a new flow-control step selected, Add put the next step at the top level, because the step had no bodies yet, while a loaded loop took it as a child. The schema's body keys now decide.
- **Files**:
  - Code: `gui/script_builder/{command_schema,step_model,step_form_view,step_list_view,builder_tab}.py`, `utils/json/json_file.py` (`write_action_json` takes a dict too) and the four catalogues.
  - Docs: `CHANGELOG.md` and `architecture_explore.md` (line counts).

## U-20260925-42 · 2026-09-25 · Every numeric line edit validates in the C locale through one helper: under French or German the Image Detect threshold could not be changed, and a grouped integer passed the validator but not int() · #bugfix #gui #i18n

- **Found with**: the Script Builder's float fields (see the previous entry). The same default-locale validators were in the core tabs.
- **Symptom**:
  - Image Detect's threshold starts at `0.8`. Under a French locale the validator refuses `.`, so no other value could be typed, and `0,9` reached `float()` as an error.
  - Under German, `QIntValidator` accepts `1.000` (checked: `Acceptable`), which `int()` rejects, in the Auto Click interval, repeat count, cursor and scroll fields and Screenshot's pixel fields.
- **Change**: new `gui/_validators.py` with `int_validator()` and `double_validator()` in the C locale. The Image Detect, Auto Click and Screenshot tabs and the Script Builder form use it. `test_gui_numeric_validators.py` (new, 3) checks both validators under `de_DE` and fails on any `QIntValidator(` / `QDoubleValidator(` in `gui/` outside the helper.
- **Files**: `gui/{_validators,_image_detect_tab,_auto_click_tab,_screenshot_tab}.py`, `gui/script_builder/step_form_view.py`, and `architecture_explore.md` (a row for the helper, and line counts).

## U-20260925-40 · 2026-09-25 · The MCP server speaks the stateless 2026-07-28 over stdio, per request beside the initialize-based revisions: server/discover, resultType and caching hints, the reserved error codes, per-request capabilities and log levels, and destructive-tool confirmation as a signed multi round-trip · #feature #mcp

- **Why**: MCP 2026-07-28 (released 2026-07-28; the Python, TypeScript, Go and C# SDKs speak it) removes the `initialize` handshake and sessions. A client of that revision sends its version and capabilities with every request, and a server that only answers `initialize` looks to it like a legacy server. `Progress.md` had it as TODO.
- **Dual-era dispatch** (`utils/mcp_server/_stateless.py`, new): a request whose `params._meta` carries `io.modelcontextprotocol/protocolVersion` is served from that request alone; `initialize` and every request without the key take the old path, so existing clients are unaffected. `MCPServer._dispatch` moved into the mixin; the handshake-era method table is now `_run_method`.
  - **Per-request fields**: `clientCapabilities` must be an object and `logLevel` a known level, otherwise `-32602`. An unsupported version is `-32022` with `data.supported` (2026-07-28, then the four handshake versions) and `data.requested`. `inputResponses` / `requestState` are type-checked the same way.
  - **`server/discover`**: `supportedVersions`, `capabilities`, and the server's identity; `-32602` without the per-request fields.
  - **Methods**: tools, resources and prompts (list / call / read / get). `ping`, `logging/setLevel` and `resources/(un)subscribe`, which the revision removed, are `-32601` there.
  - **Results**: `resultType` everywhere, `_meta["io.modelcontextprotocol/serverInfo"]`, and `ttlMs` / `cacheScope: "private"` on the cacheable ones (an hour for discover, a minute for the lists, 0 for `resources/read`, whose content is live).
- **Nothing from the connection, nothing unasked**:
  - The capabilities the gates read are the request's (`_client_capabilities` prefers the thread's stateless request).
  - `_send_outbound_request` raises inside a stateless request, so `request_sampling` / `refresh_roots` cannot send a server-initiated request.
  - `MCPLogBridge.request_scope` forwards a stateless request's records only when it set `logLevel`, at that level or above. A stdio peer whose first request was stateless gets no background records, and no `tools/list_changed` or `resources/updated` (`_unsolicited_notifier`); `initialize` switches that back.
- **Confirmation by multi round-trip** (`utils/mcp_server/_input_required.py`, new): with `JE_AUTOCONTROL_MCP_CONFIRM_DESTRUCTIVE=1`, a stateless destructive call is answered with `resultType: "input_required"`, the `elicitation/create` question under `inputRequests["confirm"]`, and a `requestState`.
  - The state is HMAC-SHA256 under a per-process key over the method, the tool and a SHA-256 of the canonical arguments. It expires after 300 s and is redeemed once, so a confirmed call cannot be replayed with the same answer.
  - A tampered, expired, reused or mismatched state is `-32602`. `decline` / `cancel` is `isError: true` and the tool does not run. A retry without an answer is asked again.
  - A client without `elicitation` gets `-32021` with `data.requiredCapabilities`. The handshake era's unprompted fallback is not carried over.
  - `needs_confirmation(tool)` is now shared by both eras.
- **Smaller**: `_MCPError` and `_error_response` carry an optional `data`.
- **Tests**: `test_mcp_2026_07_28.py` (new, 29). The 399 MCP tests pass. The HTTP transport and `subscriptions/listen` stay open in `Progress.md`.
- **Spec**: modelcontextprotocol.io/specification/2026-07-28 (changelog, versioning, `server/discover`, MRTR, caching, subscriptions, Streamable HTTP).
- **Files**:
  - Code: `utils/mcp_server/{_stateless,_input_required,server,_client_requests,_protocol,log_bridge}.py`.
  - Docs: the MCP server doc (Eng/Zh), the three READMEs, `CHANGELOG.md`, `Progress.md`, and `architecture_explore.md` (two new rows, line counts).
- **Found by**: PR #489's pytest-headless on ubuntu-22.04 and macos-14 (both Pythons) and "Headless pytest inside the image" failed from f437bcc0 on. The only failure was `test_file_drop_batch.py::test_drop_files_dispatches_packed_blob_to_driver`: `['/home/runne...\\report.pdf'] == ['C:\\docs\\report.pdf']`.
- **Cause**: U-20260925-34 made `drop_files` resolve relative paths with `os.path.abspath`. `DROPFILES` carries Windows paths, and POSIX `os.path` does not know `C:\docs\report.pdf` is absolute, so it prefixed the working directory.
- **Change**: absoluteness is now judged with `ntpath.isabs`. A path that is already absolute by Windows rules is kept as written; others still go through `os.path.abspath` (on Windows, the drive-relative and rooted cases). A simulation with `os.path` swapped for `posixpath` keeps `C:\docs\report.pdf`. `test_file_drop_batch.py` and the other file-drop tests pass (22, 1 skipped).
- **Files**: `utils/file_drop/file_drop.py`, and `architecture_explore.md` (line counts).

## U-20260925-44 · 2026-09-25 · The region selector answers in native pixels on every screen: one overlay per screen, where a single overlay offset every region by the virtual desktop's origin and left scaled screens uncovered and in logical pixels · #bugfix #gui #hidpi

- **Found by**: reading `gui/selector/`, then measured on this machine: two screens, the second at 125% and 164 px higher. Qt reports it as `(1920, -164, 1536, 864)` and Windows as `(1920, -164, 1920, 1080)`: the top-left is shared, and the size is scaled by the device pixel ratio. Nothing in the package used `devicePixelRatio`.
- **Bugs**:
  - `RegionOverlay` set its geometry to the union of all screens and then called `showFullScreen()`, which puts a window on one screen. The result still added the union's origin, so on this machine every region picked on the primary screen was 164 px too high.
  - The scaled screen had no overlay. Had it had one, the region would have been in logical pixels, while every consumer passes it to captures that use native pixels (template cropping, OCR and screenshot regions, the WebRTC capture region).
  - `pick_region_blocking` waited in a `processEvents` loop that kept a core busy.
- **Change**: one overlay per screen, each converting its rectangle with `native_region` (origin plus offset times the device pixel ratio). The wait is a `QEventLoop`. Closing an overlay any other way (Alt+F4) counts as a cancel, so the caller is not left waiting.
- **Tests**: `test_region_selector_screens.py` (new, 3) drives the selector in a child process on Qt's offscreen platform, configured with the same two screens (`offscreen:configfile=`; the path must be relative, because platform options are colon-separated). A drag answers `(100, 100, 201, 101)` on the primary screen and `(2045, -39, 251, 126)` on the scaled one. The old code answered `(100, -64, ...)` and `None`; the close case is a guard.
- **Not verified on a real desktop**: Jeffrey_RPA was running on this machine, and a full-screen top-most overlay would have covered its screen.
- **Files**: `gui/selector/{region_overlay,region_selector}.py`, and `architecture_explore.md` (line counts).

## U-20260925-45 · 2026-09-25 · The Flow Editor reads and writes action files through the Script Builder's helpers: files with a BOM open, wrapped files keep their other keys, and saves are atomic · #bugfix #gui

- **Bugs**:
  - The Flow Editor opened files with `open(..., encoding="utf-8")`, so a file saved from Notepad with a BOM failed to parse. The Script Builder reads with `utf-8-sig`.
  - It saved with a plain `open(..., "w")` (not atomic, unlike `write_action_json`), and wrote a wrapped `{"auto_control": [...]}` file back as a bare list.
  - Its inspector showed `params: {}` for a step with positional arguments.
- **Change**: new `load_action_file` / `save_action_file` in `script_builder/step_model.py` return and take the wrapped file's other keys. The Script Builder (U-20260925-41 had inlined this) and the Flow Editor both use them. The Flow Editor now also catches `AutoControlException` from the reader, and shows `Step.args`.
- **Tests**: `test_flow_editor_files.py` (new, 3). On the old code the helpers do not exist, and the Flow Editor rejects the BOM file.
- **Files**: `gui/script_builder/{step_model,builder_tab}.py`, `gui/flow_editor/tab.py`, and `architecture_explore.md` (line counts).

## U-20260925-46 · 2026-09-25 · MCP 2026-07-28 over Streamable HTTP: a stateless POST mirrors its body into MCP-Protocol-Version, Mcp-Method and Mcp-Name, a disagreeing header is 400 HeaderMismatch, version and metadata errors are 400 and unknown methods 404, and no session is kept · #feature #mcp

- **Before**: U-20260925-40 served 2026-07-28 over stdio only. Over HTTP, `_protocol_version_supported` refused the header `MCP-Protocol-Version: 2026-07-28` with a plain `{"error": ...}` 400, which a dual-era client reads as a legacy server.
- **Which requests** (`utils/mcp_server/_http_stateless.py`, new; pure functions): a POST is stateless when its `MCP-Protocol-Version` header or its body's `_meta` says 2026-07-28 (`is_stateless`).
- **Refusals before dispatch** (`stateless_refusal`), per the Streamable HTTP binding's server validation:
  - `MCP-Protocol-Version` missing or unequal to the `_meta` version is 400 `HeaderMismatch` (`-32020`). The header naming 2026-07-28 over a body without `_meta` is 400 `-32602`.
  - `stateless_request` errors (`-32022`, `-32602`) are 400 with their `data`.
  - `Mcp-Method` must equal `method`. `Mcp-Name` must equal `params.name` for `tools/call` and `prompts/get`, and `params.uri` for `resources/read`. `decode_header_value` decodes `=?base64?...?=` first and refuses a plain value outside visible ASCII, space and tab.
  - A method the revision does not have is 404 `-32601`.
- **After dispatch** (`status_for`): `-32601` is 404, and `-32020` / `-32021` / `-32022` are 400. So a destructive call from a client without `elicitation` is 400 `-32021` on a plain POST. An SSE POST has already sent 200 and carries the error in the stream.
- **No session** (`_serve_stateless`): `Mcp-Session-Id` is ignored and none is minted, and each request is scoped to its connection. Confirmation needs no stream, because the question comes back in the result. `GET` / `DELETE` naming 2026-07-28 are 405 with `Allow: POST`.
- **Any POST**: a header naming a version the server does not speak is still 400, now with a JSON-RPC `-32022` body (`supported`, `requested`, and the body's id when it has one). GET and DELETE keep the plain 400. The handshake-era path moved into `_serve_in_session` unchanged, which keeps `do_POST` under the complexity limit.
- **Tests**: `test_mcp_2026_07_28.py` +15. They cover a session-free request, ignored session ids, the four header-mismatch shapes, `Mcp-Name` including its Base64 form, the 400 / 404 / 400-with-data statuses, a confirmation round trip, GET / DELETE 405, a handshake client still getting a session, and header-value decoding. The 414 MCP tests pass. `subscriptions/listen` stays open in `Progress.md`.
- **Files**:
  - Code: `utils/mcp_server/{_http_stateless,http_transport,_stateless}.py`.
  - Docs: the MCP server doc (Eng/Zh), the three READMEs (no longer "stdio"), `CHANGELOG.md` (Added text, and Changed for the 400 body), `Progress.md`, and `architecture_explore.md` (new row, line counts).

## U-20260925-47 · 2026-09-25 · The MCP server's subscription handlers move from server.py into _subscriptions.py, before subscriptions/listen joins them; no behaviour change · #refactor #mcp

- **Why**: `server.py` was at 744 lines against the 750 limit, and `subscriptions/listen` (the next entry) adds to the same concern. The code it would join belongs in its own module anyway.
- **Moved into the `SubscriptionMixin`**, unchanged: `_handle_resources_subscribe`, `_handle_resources_unsubscribe`, `_notify_resource_updated` and `_notify_tools_list_changed`. They now live in `utils/mcp_server/_subscriptions.py` (new, 83 lines), with the `functools` import they use.
  - `MCPServer` is now `(StatelessDispatchMixin, SubscriptionMixin, ClientRequestMixin)`.
  - The state (`_resource_subscriptions`, `_subscriptions_lock`) stays in `MCPServer.__init__`.
  - `server.py` is 691 lines.
- **Refactor only**: no behaviour, name or signature a caller sees changes. The 414 MCP tests pass before and after.
- **Files**: `utils/mcp_server/{server,_subscriptions}.py`, `architecture_explore.md` (new row, line counts).

## U-20260925-48 · 2026-09-25 · MCP 2026-07-28 subscriptions/listen over stdio and HTTP: an acknowledgement of what the server will send, notifications tagged with the subscription id, cancellation by notifications/cancelled or a closed stream, and a completion answer when the server ends it; the 2026-07-28 Progress item is done · #done #mcp

- **Why**: 2026-07-28 replaces `resources/subscribe` and the HTTP GET stream with `subscriptions/listen`, and says every implementation must support its message patterns. Until now it was `-32601` (HTTP 404), and a stateless client had no way to learn about tool-list changes or screen updates. This was the last open part of the `Progress.md` item, which is deleted (#done).
- **Opening** (`_listen` in `utils/mcp_server/_subscriptions.py`):
  - The `notifications` filter must be an object with boolean flags and a list of string `resourceSubscriptions`, otherwise `-32602`. Another listen on an open id, or an id that is not a string or number, is `-32600`.
  - The first message is `notifications/subscriptions/acknowledged`, with the part the server will honour. `toolsListChanged` is kept. `promptsListChanged` and `resourcesListChanged` are left out, since those lists never change. Only URIs a provider accepted are kept (`autocontrol://screen/live` today), with duplicates dropped.
  - A `Listener` holds each subscription's emit and a lock. Nothing on a subscription goes out before its acknowledgement, even if a provider fires at once.
- **Delivery**: every notification carries `_meta["io.modelcontextprotocol/subscriptionId"]`, the listen request's id.
  - `register_tool` / `unregister_tool` notify subscriptions that asked for `toolsListChanged`, and still notify a handshake peer as before.
  - Each subscribed URI gets its own provider handle, so several subscriptions to one resource are independent.
  - A failed write ends the subscription and releases its handles.
- **Ending**:
  - `notifications/cancelled` naming the listen id (stdio), or the client closing the stream (HTTP), ends it silently.
  - The server ending it answers the listen request with `{"resultType": "complete", "_meta": {subscriptionId}}` first: `serve_stdio` finishing calls `end_subscriptions(stdio=True)`, and `HttpMCPServer.stop()` calls `end_subscriptions(stdio=False)`.
  - `forget_connection` ends a connection's leftovers.
- **Wiring**:
  - `_dispatch` routes the method inside the stateless scope. The handler returns `NO_RESPONSE`, which `_build_response` turns into no reply. `STATELESS_METHODS` now holds `server/discover` and `subscriptions/listen` too.
  - `STATELESS_CAPABILITIES` declares `tools.listChanged` and `resources.subscribe`.
- **HTTP** (`_stream_listen`): it needs `Accept: text/event-stream`, otherwise 406 with `-32600`. The response is SSE with `X-Accel-Buffering: no` and no session. A keep-alive comment every 15 s surfaces a vanished client as a write error.
- **Tests**: `test_mcp_2026_07_28.py` +13, including the acknowledgement subset, tagging, silent cancellation and resource release, the completion answer over stdio (`serve_stdio` end to end) and HTTP (`stop()`), a client closing its stream, and 406. The file (57) ran three times in a row without a failure. The 440 MCP and doc tests pass, and the typing contract is clean on all three platforms.
- **Files**:
  - Code: `utils/mcp_server/{_subscriptions,_stateless,_http_stateless,http_transport,server}.py`.
  - Docs: the MCP server doc (Eng/Zh), `CHANGELOG.md`, `Progress.md` (item removed), and `architecture_explore.md` (row, line counts).

## U-20260925-51 · 2026-09-25 · Python 3.15 readiness: Windows console output is decoded in its code page under the default UTF-8 mode, and the SBOM skips distributions that have no metadata · #bugfix #compat

- **Why**: Python 3.15.0 is due on 2026-10-01 (rc2 is out). Its What's New lists two changes that reach this package.
- **UTF-8 mode by default (PEP 686)**:
  - In UTF-8 mode, `locale.getpreferredencoding(False)` answers `utf-8`, and `subprocess.run(text=True)` decodes UTF-8. `cmd`, `sc` and `ipconfig` still write the ANSI code page (cp950 on Traditional Chinese Windows).
  - `shell_exec.py` already records the symptom from an earlier fix: `磁碟區` came back as replacement characters.
  - Four readers relied on the locale default: `AC_shell_to_var` (`flow_data_commands.py`), the MCP `shell_command` tool (`_handlers_system.py`), `ShellManager.program_encoding`, and the remote host's `status` command (`sc query` with `text=True`). The last one raised `UnicodeDecodeError`, which its `except (OSError, SubprocessError)` did not catch.
  - The new `shell_exec.console_encoding()` returns `locale.getencoding()` (3.11+, ignores UTF-8 mode) on Windows, and the preferred encoding elsewhere. That is the same answer as before on 3.10–3.14, where UTF-8 mode is off. `sc query` now passes it with `errors="replace"`.
- **`importlib.metadata`**: on 3.15 `Distribution.metadata` raises `MetadataNotFound` (a `FileNotFoundError`) for a distribution without a METADATA file, such as a directory left by an interrupted uninstall. `build_sbom` caught only `KeyError` / `AttributeError`, so one such leftover ended the whole SBOM. On 3.14 it was listed as `unknown` at version `0`, with a `DeprecationWarning` for the implicit `None`.
  - `sbom._name` reads the name with `.get` and treats a missing file as no name. Distributions without one are skipped, in the full inventory and in a dependency closure.
- **Tests**:
  - `test_console_encoding.py` (new, 5) simulates UTF-8 mode on Windows on any interpreter. 4 of 5 fail on the old code.
  - `test_sbom_missing_metadata.py` (new, 2) runs with warnings as errors and simulates both the 3.15 and the 3.14 shape. Both fail on the old code.
- **Files**:
  - Code: `utils/shell_process/shell_exec.py`, `utils/executor/flow_data_commands.py`, `utils/mcp_server/tools/_handlers_system.py`, `utils/remote_desktop/host_service.py`, `utils/sbom/sbom.py`.
  - Docs: the v4 features doc (Eng/Zh, `AC_shell_to_var`'s default encoding), `CHANGELOG.md`, `architecture_explore.md` (line counts).
## U-20260925-49 · 2026-09-25 · Remote desktop input lands where the viewer clicked: both hosts dispatch viewer coordinates relative to the captured frame's origin, and the legacy host broadcasts its cursor in frame coordinates · #bugfix #remote-desktop #multi-monitor

- **Found by**: following the region-selector fix (U-20260925-44) through the hosts. The WebRTC track already draws its cursor at `cursor - monitor.left/top`, so frame coordinates are relative to the capture origin. No host added that origin back before moving the pointer.
- **Bugs**:
  - Every host passed the viewer's frame coordinates to `set_mouse_position` as screen positions. A click on a frame that does not start at (0, 0) landed that far off: a second monitor, a `region`, or the legacy host's default full capture, whose frame starts at the virtual desktop's corner. On this machine that corner is (0, -164), because a monitor sits above the primary one.
  - The legacy host broadcast `get_mouse_position()` as is, so the viewer drew the remote cursor off by the same amount.
- **Change**:
  - `dispatch_input(message, origin=(x, y))` adds the origin to absolute `x` / `y`. `dispatcher_at(origin_of)` asks for the origin on every message, because the captured monitor can change mid-session.
  - `RemoteDesktopHost` takes the origin from its region, or from the virtual desktop via `capture_origin()` (`mss` monitor 0), and reports the default cursor relative to it. A custom frame provider or dispatcher is left as it was.
  - `ScreenVideoTrack.capture_origin` records the last resolved monitor's or region's corner. `WebRTCDesktopHost` dispatches by its own track's origin, and `MultiViewerHost` by the shared track's, which its relayed sessions cannot see.
  - If `mss` cannot read the layout (`ScreenShotError`, no display), the origin falls back to (0, 0) and the host still starts.
- **Tests**: `test_rd_capture_origin.py` (new, 6) fails 6/6 on the old code. The 582 remote-desktop tests touching these hosts pass.
- **Files**: `utils/remote_desktop/{input_dispatch,host,host_capture,webrtc_transport,webrtc_host,multi_viewer}.py`, and `architecture_explore.md` (line counts).

## U-20260925-50 · 2026-09-25 · Host annotations are drawn where the viewer drew them: the hosts stamp each event with the frame's screen origin, and the overlay moves to that screen and converts native pixels to its logical ones · #bugfix #remote-desktop #hidpi

- **Bug**: the viewer draws in frame pixels. `HostAnnotationOverlay` covered the primary screen and painted those pixels as its own logical ones. So strokes were off by the frame's origin (another monitor or a region), and on a scaled screen by the device pixel ratio as well.
- **Change**:
  - `WebRTCDesktopHost` stamps every annotation with `screen_origin` from its track, overwriting any value the viewer sent. `MultiViewerHost` stamps it with the shared track's origin, because its sessions relay that track.
  - The overlay moves to the screen holding the point (and clears strokes left on the previous screen), then converts native to logical pixels.
  - The conversions are shared in a new `gui/_screen_geometry.py`: `native_region`, moved from the region selector, plus `screen_at_native` and `logical_point`.
- **Tests**: `test_rd_annotation_screens.py` (new, 3) runs the overlay in a child process on the offscreen platform with the two-screen layout used for U-20260925-44. A point at frame (10, 20) with origin (1920, -164) lands at (8, 16) on the 125% screen. The test also checks that both hosts overwrite a viewer-sent origin. All 3 fail on the old code.
- **Files**: `gui/_screen_geometry.py` (new), `gui/remote_desktop/annotation_overlay.py`, `gui/selector/region_overlay.py`, `utils/remote_desktop/{webrtc_host,multi_viewer}.py`, and `architecture_explore.md` (a row for the new module, and line counts).

## U-20260925-52 · 2026-09-25 · Screen-coordinate conversions no longer scale by the device pixel ratio on macOS, where captures and the pointer take points; and measured: importing the package makes a Windows process system-DPI-aware · #bugfix #hidpi #macos

- **Bug**: U-20260925-44 (region selector) and U-20260925-50 (host annotations) convert Qt's logical pixels to screen coordinates by multiplying by the device pixel ratio. On macOS, `mss`, `screencapture` and Quartz events all take points, which is Qt's logical unit, so a Retina display (ratio 2) doubled every selected region.
- **Change**: `gui/_screen_geometry.capture_ratio(screen)` is 1.0 on macOS and the device pixel ratio elsewhere. `native_region`, `screen_at_native` and `logical_point` all use it. `test_region_selector_screens.py` gains a case that runs the two-screen probe with `sys.platform` set to `darwin`: `(2020, -64, 201, 101)`, unscaled.
- **Measured on Windows**: `import je_auto_control` makes the process system-DPI-aware, through `SetProcessDPIAware()` in `windows/screen/win32_screen.py` (`GetAwarenessFromDpiAwarenessContext` = 1). Qt then reports the 125% screen as `(1920, -164, 1536, 864)` at ratio 1.0, which is the same virtualised space as `mss` and the cursor. So in the GUI the conversions reduce to the origin offset, which still fixes the 164-pixel error measured on this machine. The ratio applies in per-monitor-aware processes and on Linux with Qt scaling. The awareness call is in a module Jeffrey_RPA uses and was not changed.
- **Files**: `gui/_screen_geometry.py`, `test/unit_test/headless/test_region_selector_screens.py`, and `architecture_explore.md` (line counts).
## U-20260925-55 · 2026-09-25 · Semantic replay no longer moves clicks to the wrong control, a failed heal is a failed step, OpenCV errors from video regions and odd images stay in the executor, 16-bit scans are not blackened, the HTML report is linear, and D-Bus unescapes addresses and rejects values it cannot marshal · #bugfix #audit #recording #vision

A read-only audit agent found these in five `utils/` subpackages, each reproduced with a probe and re-checked here against the current tree. `test_recording_media_audit.py` (new, 8) fails 8/8 on the old code, and the 318 existing tests for these modules pass.

- **Semantic replay**, high (wrong clicks):
  - `_default_a11y_locator` turned a recorded empty name into "any name", so a click on an unnamed icon was moved to the app's Close button. An unnamed anchor now does not relocate.
  - A match with no rectangle (a hidden tab page) relocated the click to (0, 0). It now keeps the recorded point.
  - Enrichment walked only the default 200 elements, which starts at the window, so a control further down was never seen, the window became the anchor, and replay clicked the window's centre. It now walks `SCAN_LIMIT` elements and never anchors on a container role (window, frame, dialog, pane).
- **Self-healing**: `_heal` ran outside the step's error handling. A VLM request error, or a returned NaN, escaped `replay()` and lost the steps already run. A failed heal is now a failed step, with `last_error` "heal failed: ...".
- **Video motion**:
  - An off-frame, negative or zero-width `region` sliced an empty frame, and `cv2.error`, which is not in the executor's catch list, aborted the whole script. The region is now clipped, one with nothing left is a `ValueError`, and OpenCV errors are contained.
  - Every frame of the segment was kept in memory (about 37 GB for ten minutes of 1080p30). The differences are now summed as the frames are read.
- **Preprocessing**:
  - Images are converted to uint8 on input. A 16-bit PNG used to stay 16-bit, so Otsu wrote 0 and 255 into a uint16 image (black) and adaptive thresholding raised.
  - `(H, W, 1)` arrays and float images now work.
  - `block_size < 3` and `grid < 1` are `ValueError`, every public function turns `cv2.error` into `AutoControlScreenException`, and a comma-separated `steps` string is split (`"deskew"` was walked letter by letter).
- **HTML report**: each record was appended to the whole report so far, which is quadratic (42 s for 16,000 records). The tables are now joined once; `make_html_table` keeps its signature.
- **D-Bus**: address values are percent-unescaped (`/run/bus-for-%3A0` is `/run/bus-for-:0`). Out-of-range numbers (`u` = -1, `y` = 300, which used to go out as 44) and truncated signatures (`"(i"`, `"a"`) raise `DBusError` instead of `struct.error` / `IndexError`.
- **Files**: `utils/{semantic_recording/{replay,enrich,self_healing},media_assert/media,preprocess/preprocess,generate_report/generate_html_report,dbus_client/session_bus}.py`, and `architecture_explore.md` (line counts).

## U-20260926-01 · 2026-09-26 · Perceptual diff keeps an empty image in the executor and fails a zero budget on one pixel, text regions answer in screen coordinates, match_rotated_all finds every copy, actionability reads falsy probes and NumPy tokens, the generated project's example validates, and grid fill counts a spanning box once · #bugfix #audit #vision

A read-only audit agent found these in eight `utils/` subpackages, each reproduced with a probe and re-checked here. `test_vision_grid_audit.py` (new, 9) fails 9/9 on the old code, and the 198 existing tests for these modules pass.

- **Perceptual diff**:
  - A 0-byte image made OpenCV raise `cv2.error`, which escaped `execute_action` and aborted the script. Both functions now contain it (`AutoControlScreenException`).
  - `diff_ratio` is rounded to six decimals, so one changed pixel in a 1080p frame (up to four at 4K) read 0.0 and passed `max_diff_ratio=0.0`. The budget is now checked on the unrounded ratio.
- **Text regions**: `find_text_regions` and `find_text_lines` dropped the capture origin and answered region-local boxes, `(20, 18)` for text at `(520, 318)`. They now use `_haystack_gray_with_origin` and `_to_screen`, as the other locators do.
- **Rotated matching**: `match_rotated_all` kept one peak per (scale, angle), so three copies of a template came back as one. It now collects every position above `min_score` per pose, with the same cap as `match_template_all`, before NMS.
- **Actionability**:
  - `enabled_probe() is not False` read NumPy's `False_` and a Win32 0 as enabled, so a greyed-out control was clicked. `None` still means unknown; anything else is judged by its truth value.
  - A NumPy pixel sample (or bbox) in the stability token raised "truth value of an array is ambiguous" on the second poll. Tokens are now compared by value.
  - With `stable_for_s=0`, a `timeout_s=0` check could never pass. It now can.
- **Generated project**: `keyword1.json` used `time_sleep` (registered only at run time) and `print`, so the executor's up-front validation rejected the whole file, and both example runners failed on first run. It now uses `AC_sleep`. `bad_keyword_1.json` is still the deliberately failing example.
- **Grid fill**:
  - A box straddling two cells landed in the cell under its centre and was also reported as a span anchored at the other cell. Cells and spans now share one rule: the cells under the middle half of the box. A spanning box goes to its anchor cell, and a box crossing a rule by a sliver stays in its cell.
  - Tesseract's `left/top/width/height` boxes are read, and a box missing `y` raises the documented `ValueError` instead of `KeyError`.
- **Files**: `utils/{perceptual_diff/perceptual_diff,text_regions/text_regions,rotated_match/rotated_match,actionability/actionability,project/template/template_keyword,table_grid_fill/table_grid_fill}.py`, and `architecture_explore.md` (line counts).

## U-20260926-02 · 2026-09-26 · One reader for element geometry in every shape (set-of-marks dropped OCR boxes, consensus put accessibility elements at (0, 0)), finite consensus points, columns left of x = 0, ${var} sleeps in the recording editors, wrapped files in the flow debugger, and a framework name as a string · #bugfix #audit #vision

A read-only audit agent found twelve defects in eleven `utils/` subpackages, each reproduced with a probe and re-checked here. Eight are fixed below. The other four are in modules that Jeffrey_RPA calls in its running batch (`text_unicode` is loaded by the keyboard wrapper; `keyboard_layout` and `clipboard_formats` are called from `_gui_control.py`), so they are recorded in `Progress.md`'s section for that batch. `test_layout_consensus_audit.py` (new, 14) fails 14/14 on the old code, and the 805 existing tests for these modules pass.

- **Element geometry**: each module read one shape. `set_of_marks` read only `bbox` / `bounds`, so OCR matches and fused elements, which are `x/y/width/height` and meant as its input, got no marks. `grounding_consensus` read only `x/y/width/height`, so accessibility elements (`bounds`) and set-of-marks output (`bbox`) all sat at (0, 0), and the first of them won with full agreement. `i18n_test` had a copy of the first reader. The new `element_box()` in `accessibility/element.py` reads every shape, from dict keys or attributes, and all three use it. Consensus skips elements with no geometry and returns `None` when none have any.
- **Consensus points**: weights were checked for finiteness but coordinates were not. `inf` raised `OverflowError` and `NaN` raised `ValueError` from the centroid. Both are now refused up front, with the weight check's `ValueError`.
- **Columns**: `vertical_projection` clamped boxes to x ≥ 0. On a monitor left of the primary one, the ink was lost, or the real gutter was taken for a left margin, and the table came back `None`. The profile now starts at the leftmost edge, and gutters are reported in screen coordinates.
- **Recording editor**: `adjust_delays` and `merge_sleeps` ran `float()` on a `${wait}` delay, which the executor interpolates at run time, and raised `ValueError`. Such a sleep is now left alone and not merged.
- **Flow debugger**: `list()` turned the executor's `{"auto_control": [...]}` form into `["auto_control"]`, and both `trace_actions` and `FlowDebugger` failed validation. They now unwrap it as the executor does.
- **Compliance report**: `frameworks="SOC2"` was walked letter by letter ("unknown frameworks 2, C, O, S"). It is now one name. Evidence that is not a mapping is a `ValueError` instead of a `TypeError`. The framework check moved into `_wanted_frameworks` to keep the builder under the complexity limit.
- **Files**: `utils/{accessibility/element,set_of_marks/set_of_marks,i18n_test/i18n_test,grounding_consensus/grounding_consensus,column_layout/column_layout,recording_edit/editor,flow_debugger/flow_debugger,compliance/compliance_report}.py`, `Progress.md` (the four held findings), and `architecture_explore.md` (line counts).

## U-20260926-03 · 2026-09-26 · AC_resolve_ref / ac_resolve_ref no longer return secret:// values into executor records and MCP results; colour matches are in screen coordinates and ignore blank backgrounds; mark labels stay beside their marks; JSON snapshots match their own payload; config schemas reject null, containers and typo'd types · #security #bugfix #audit

Three read-only audit agents found these, each reproduced with a probe and re-checked here. `test_secret_color_marks_audit.py` (new, 16) fails 15/16 on the old code; the 16th guards that `env://` still resolves. The 140 existing tests for these modules pass.

- **Secrets in records**, security: `AC_resolve_ref` / `AC_resolve_refs` resolved `secret://` through the credential broker and returned the value as `{value}`. That put it in the executor record and in the result of the MCP tools `ac_resolve_ref` / `ac_resolve_refs`, which are annotated read-only. The broker's contract is that secret values never enter those records. Both adapters now refuse `secret://` (anywhere in the structure) with `SecretRefError`, pointing to `${secrets.NAME}`, which injects the value into the step that needs it. The Python `resolve_ref` API is unchanged. A non-string ref, a path with a NUL byte and a resolver that misses with `KeyError` are now `SecretRefError` too. Whether MCP clients may read `env://` and `file://` is the same question as the existing Progress item on MCP path roots, and is recorded there.
- **Colour matching**:
  - `match_color` / `match_color_all` answered region-local coordinates, so `AC_match_color`'s `center` clicked that far off. They now add the region's corner.
  - Every template pixel was scored. A thin coloured glyph on white is mostly background, so a red cross matched a blank white screen at 0.78. Scoring now uses the template's chromatic pixels (saturation 40 or more), or every pixel for an achromatic template.
  - `match_color_all` sent every position above `min_score` to a quadratic NMS: 18 s at 320x240, and past five minutes at 640x480. Candidates are now capped as `match_template_all` caps them.
  - OpenCV errors are contained.
- **Mark labels**:
  - A mark on a monitor left of or above the primary had its label clamped to 0, up to 1,500 px away. Labels now go below 0 only where their mark does.
  - Crowded marks near the right edge were all given the same out-of-bounds label. The fallback now searches label-sized steps in every direction inside the bounds.
  - Marks shaped `{bounds}` or `{x, y, width, height}` raised `KeyError('bbox')`. They are read through `element_box`.
- **JSON contracts**:
  - `snapshot_json` stored the JSON form but compared the raw payload, so tuples, integer keys and NaN never matched their own snapshot.
  - `match_json(ignore="$.ts")` built `set("$.ts")`, whose `"$"` prefixes every path, so every difference was ignored and the contract always passed. A string is now one path, tuples are arrays, and NaN equals NaN.
- **Config schemas**:
  - `None` and lists passed a `str` field as `"None"` / `"[1, 2]"`, and `Decimal("2.9")` was truncated to 2 in an `int` field.
  - An unknown type (`"integer"`) returned the value unchecked. It now raises `ValueError` when the schema is built.
  - `"required": "false"` read as required.
- **Also recorded in `Progress.md`** (DECIDE): `import je_auto_control` makes a Windows process only system-DPI-aware, so a monitor at another scale is virtualised (measured: 1536x864 reported for a 1920x1080 panel at 125%). Changing that is on Jeffrey_RPA's live screenshot path.
- **Files**: `utils/{secret_ref/{secret_ref,__init__},executor/action_executor,color_match/color_match,marks_layout/marks_layout,json_contract/json_contract,config_schema/config_schema}.py`, the v85 feature doc (Eng/Zh), `Progress.md`, and `architecture_explore.md` (line counts).
## U-20260926-04 · 2026-09-26 · Nested action lists run on the executor running them; dark-theme widgets classify; ß collates as ss; checksums take whole floats and any length; the IME wait keeps its deadline; TF-IDF keeps common terms and CJK words are searchable; unique ccorr matches are trusted; OCR text satisfies postconditions; reading flow takes every element shape · #bugfix #audit

Read-only audit agents found these, each reproduced with a probe and re-checked here. `test_icons_search_trust_audit.py` (new, 12) fails 12/12 on the old code. The 400 existing tests for these modules pass.

- **Nested action lists**: `AC_circuit_call`, `AC_with_modifiers`, `AC_bulkhead_run`, `AC_expect_poll`, `AC_replay_trace`, the chaos probes and `AC_observe` handlers ran their inner list on the module's executor. A list run on another `Executor` lost its variables inside them: every device in `run_on_devices` failed `${device.serial}` with `Unknown variable`. `Executor.execute_action` now records itself per thread and those adapters run on it. A non-object device spec raised `AttributeError` after earlier devices had run; it is now a `ValueError` naming the index, before any device runs.
- **Icon classification**:
  - Otsu with `THRESH_BINARY_INV` takes dark ink on light, so on a dark theme the background was the ink: a checkbox read as an icon, a text field as a button. The foreground is flipped when it covers the patch border.
  - A box past the image edge measured its requested aspect, not the clipped patch's.
  - A float or 16-bit image raised a bare `cv2.error`; it is an `AutoControlScreenException`.
- **Collation**: `ß` folds to `ss` but was weighed as one `s`, so `Maß` equalled `MaS`. Every character of the fold is weighed, and case is whether a character lowers.
- **Checksums**: `luhn_validate(79927398713.0)` counted the `.0`'s zero as a digit; a whole float is its integer now and a fractional one is a `ValueError`. MOD 97-10 on more than 4,300 digits hit `int()`'s limit; it is computed nine digits at a time.
- **IME wait**: `wait_for_composition_commit` slept a full `interval_s` past its deadline (a 10 s interval made a 1 s wait take 10 s), raised on a negative interval and spun on 0. It clamps the interval like the other poll loops and never sleeps past the deadline.
- **Search**:
  - TF-IDF used `log(N/df)`, which is 0 for a term in every document, so the positive-score filter dropped every match of a common term. It is smoothed to `(1 + log tf) * (log(N/df) + 1)`.
  - Kana, CJK and Hangul are written without spaces, so a sentence was one term and a word inside it (`登入` in `請先登入系統`) was never found. Those runs are indexed as character bigrams; other scripts stay whole.
  - Ties sorted by raw `doc_id` raised `TypeError` for mixed id types.
- **Match trust**: for `ccorr_normed` the peak-to-sidelobe statistics were measured against zero, and a flat UI scores high everywhere, so a unique button read as ambiguous. The sidelobe is rebased on the score map's mean for correlation methods.
- **Postconditions**: only `name` was read, so an OCR frame (`text`) never satisfied `text_present` or a name criterion. `text` and `value` are read too.
- **Reading flow**: elements shaped `{bbox}`, `{bounds}` or match objects raised; they are read through `element_box`.
- **Docs**: the v64 search feature doc (Eng/Zh) describes the bigram terms and the smoothed TF-IDF.
- **Files**: `utils/{executor/action_executor,device_matrix/matrix,icon_classify/icon_classify,locale_collation/locale_collation,checksum/checksum,ime_state/ime_state,search_index/search_index,match_trust/match_trust,postcondition/postcondition,reading_flow/reading_flow}.py`, the v64 feature doc (Eng/Zh), and `architecture_explore.md` (line counts).

## U-20260926-05 · 2026-09-26 · Script Builder region hints name the convention each command reads: [x, y, width, height] for the 27 that capture through grab_logical, [left, top, right, bottom] for AC_region_contrast, AC_read_qr and AC_region_color_stats · #bugfix #gui #script-builder

The matchers, OCR and most vision commands capture through `monitor_layout.grab_logical`, which takes `[x, y, width, height]`. The colour, histogram, SSIM and QR commands capture through `pil_screenshot` or a PIL bbox, which take `[left, top, right, bottom]`. Every Script Builder `region` hint said left, top, right, bottom, including the 25 commands of the first kind, and `AC_region_contrast` said `[x, y, w, h]`. A region typed as the hint asked therefore searched another rectangle: `[100, 100, 400, 300]` meant to be 300 x 200 searched 400 x 300 from (100, 100).

- Measured, not read: `test_script_builder_region_hints.py` (new, 47) runs every command that has a `region` field with both captures stubbed, and record which one received the step's region. It fails 30/47 on the old schema.
- **Changed hints**:
  - To `[x, y, width, height]`: `AC_auto_threshold`, `AC_detect_scale`, `AC_edge_match`, `AC_feature_match`, `AC_find_grid`, `AC_find_lines`, `AC_find_rectangles`, `AC_find_separators`, `AC_find_shapes`, `AC_find_text_lines`, `AC_find_text_regions`, `AC_image_quality`, `AC_match_auto`, `AC_match_ensemble`, `AC_match_masked`, `AC_match_rotated`, `AC_match_subpixel`, `AC_match_template`, `AC_match_with_trust`, `AC_most_salient`, `AC_quality_gate`, `AC_read_barcodes`, `AC_salient_regions`, `AC_scale_sweep` and `AC_wait_actionable`. `AC_ocr_to_var`'s example `[0, 0, 400, 80]` becomes the named form too.
  - To `[left, top, right, bottom]`: `AC_region_contrast` (was `[x, y, w, h]`); `AC_read_qr` and `AC_region_color_stats` (were bare examples).
- Commands whose region never reaches either capture (the screen grid, the ImageGrab-based colour wait, the OCR-engine path) are listed in the test with the convention their adapter reads; the grid's is checked directly.
- Scope: only the hints change. Both conventions stay as they are, since action files already rely on them.
- **Files**: `gui/script_builder/command_schema.py`, the new test, and `architecture_explore.md` (line counts).
## U-20260926-06 · 2026-09-26 · Region captures reach every monitor on Windows (colour, HSV, histogram, SSIM, contrast, preprocess, colour waits, QR, colour stats, VLM, MCP screenshot) and colour blobs found in a region are in screen coordinates; AC_wait_actionable samples stability where the match is · #bugfix #multi-monitor #vision

`pil_screenshot(screen_region=...)` hands the box to `ImageGrab.grab(bbox=...)`. Reading Pillow's Windows branch: without `all_screens=True` it captures the primary monitor only and crops that, filling the rest with black. Every command that read a `[left, top, right, bottom]` region through it therefore measured a black image on any monitor left of or above the primary. The matchers and OCR already capture through `grab_logical`, which spans every monitor in mouse coordinates.

- **New** `cv2_utils/region_capture.grab_screen_region(region)`: on Windows a region goes through `grab_logical`, converted to x, y, width, height and validated as before. Elsewhere, and for the whole screen, it is `pil_screenshot` unchanged, since macOS `screencapture -R`, the X11 root and the Wayland layout already reach every display.
- **Routed through it**: `color_region` (and with it `color_match`, `contrast_map`, `hsv_segment`, `img_histogram`), `ssim`, `preprocess`, the smart waits' sampler (`wait_until_color`, `wait_until_screen_stable`), `AC_read_qr`, `AC_region_color_stats`, the VLM capture, and the MCP `ac_screenshot` region path.
- **Screen coordinates**: `find_color_regions`, `segment_hsv` and `dominant_hue_regions` answered region-local boxes, so `AC_find_color_region`'s `center` clicked that far off, as `match_color` did before U-20260926-03. Blobs from a grabbed region now add its corner. Blobs in a supplied `haystack` keep that image's pixels. `connected_boxes` takes an `origin`, and `color_match` reuses `color_region`'s `_origin`.
- **`AC_wait_actionable`**: the stability token sampled the matched box through `pil_screenshot`, although the box is in `grab_logical` coordinates. A target on another monitor hashed black every time and read as stable at once. It samples through `grab_logical` now.
- `test_region_capture_monitors.py` (new, 8) fakes Pillow's Windows behaviour with a monitor left of the primary. 5 of the 6 tests that do not need the new module fail on the old code; the sixth guards the supplied-haystack coordinates. The 830 existing tests for these modules pass; `test_secret_color_marks_audit.py` and `test_script_builder_region_hints.py` stub the new seam; the helper is annotated `Image.Image` like `pil_screenshot`, which keeps `ac_screenshot` out of the contract sweep as before (its monitor path is not stubbable).
- **Still primary-only, recorded in `Progress.md`** (BLOCKED, Jeffrey_RPA's live path): `pil_screenshot` / `screenshot` / `AC_screenshot` with a region, `keyword_screenshot`, `capture_window`, and the set-of-marks overlay.
- **Docs**: the v128 and v139 feature docs (Eng/Zh) state the region convention and the coordinate space.
- **Files**: `utils/cv2_utils/{region_capture (new),blobs}.py`, `utils/{color_region/color_region,color_match/color_match,hsv_segment/hsv_segment,ssim/ssim,preprocess/preprocess,smart_waits/waits,vision/vlm_api,mcp_server/tools/_handlers_screen,executor/action_executor}.py`, two tests, the feature docs, `Progress.md`, and `architecture_explore.md`.

## U-20260926-07 · 2026-09-26 · Drift tests refuse NaN; profiles keep mixed, huge and bool columns honest so inferred schemas accept their rows; short and 16-bit lines are found; 16-bit screenshots annotate in tone; runs killed mid-flight no longer hide history from test selection, sharding and flakiness; 0 s steps can regress; mining and baggage refuse empty inputs · #bugfix #audit #data

A read-only audit agent found these, each reproduced with its probe and re-checked here. `test_drift_profile_lines_audit.py` (new, 22) fails 22/22 on the old code; the 602 existing tests for these modules pass.

- **Drift**: NaN compares false with everything, so sorting and bisecting left it wherever it fell. One NaN in `current` moved the KS statistic from 0.01 to 0.49 depending only on its position, and PSI put it in the last bucket. `psi` / `ks_two_sample` / `detect_drift` now raise `ValueError` naming the sample and index.
- **Profiles and inferred schemas**:
  - A mixed column (`30`, `"unknown"`, `41`) was inferred as `str`, so `validate_rows` rejected the rows the schema came from. It is `mixed` now and gets no `type` rule.
  - A column with no finite value got bounds of `None`, which crashed `validate_rows`.
  - Two `1e308` values overflowed `fsum` and a `10**400` overflowed `float`, aborting the profile.
  - Int columns had their bounds taken through `float`, rounded past 2**53, so the inferred `max` rejected the column's own largest value. They are exact now, and the mean is `None` past the float range.
  - `True` and `1` were one value in `distinct` / `top_values` and duplicates under `unique`. They count apart in both the profile and the validator.
- **Lines**: Hough's fixed 50-vote threshold dropped every line under ~50 px whatever `min_length` asked for. It follows `min_length / 2`, bounded 10..50. 16-bit and float images raised `cv2.error` in Canny; they are read as 8-bit, and OpenCV errors are contained.
- **Annotations**: `convert("RGBA")` clips 16-bit and float images to 0..255, so a 16-bit screenshot came out pure black and white. They are scaled to 8 bits first.
- **Run history consumers**: runs killed mid-flight stay `running` forever. More of them than a fixed 16-row head-room hid a flow's whole history, so `rank_flows` scored it as never run (0.8) and `shard_flows` gave it the default weight. `analyze_flakiness` counted them toward `limit`. `HistoryStore.list_runs` takes `statuses`, and all three read `FINISHED_STATUSES` only. `rank_flows` also accepts a float `window` (it raised `TypeError`), refuses a window below 1, and ranks a flow listed twice once.
- **Run diffs**: a step at 0 s was never compared, so 0 s to 30 s was not a regression. It is compared as 0.1 s with `ratio` `None`. Non-finite durations are skipped, so no `inf` ratio reaches a JSON report.
- **Mining**: `min_len` / `max_len` / `min_count` of 0 or less counted the empty sequence as a pattern. They now raise `ValueError`.
- **Baggage**: an empty key formatted as the member `=x`, which no parser reads back. `Baggage` refuses it.
- **Test hygiene**: `test_wait_for_port.py`'s accept thread could reach `accept()` after the test closed the socket and warned `PytestUnhandledThreadExceptionWarning` (seen once in a full run). The test joins it first.
- **Docs**: the v11, v47, v77, v82, v84, v141 and v192 feature docs (Eng/Zh).
- **Files**: `utils/{data_drift/data_drift,data_profile/data_profile,data_quality/data_quality,edge_lines/edge_lines,annotate/annotate,test_select/test_select,test_shard/test_shard,flakiness/flakiness,run_history/history_store,run_diff/run_diff,process_mining/process_mining,baggage/baggage}.py`, the new test, `test_wait_for_port.py`, the feature docs and `architecture_explore.md` (line counts).
## U-20260926-08 · 2026-09-26 · json_contract checks NaN with math.isnan instead of a self-comparison, which SonarCloud read as a constant-false condition and failed PR #490's reliability gate on · #quality #sonar

SonarCloud's gate on PR #490 went to `new_reliability_rating` 3: four findings on `json_contract.py:34`, `python:S1764` (identical sub-expressions on both sides of `!=`) and `pythonbugs:S2583` (condition always false), all on `left != left and right != right`, the NaN test added in U-20260926-03. The self-comparison was correct for floats, but it reads as a typo and the checker cannot tell. It is `_is_nan(value)` now (`isinstance(value, float) and math.isnan(value)`), with the same behaviour. The 54 `json_contract` tests pass, including the NaN-equality case.

- **Files**: `utils/json_contract/json_contract.py`.

## U-20260926-09 · 2026-09-26 · Region captures on macOS stay in points on Retina (scale_down=True), so colour blobs and other region results line up with the mouse; the matchers' grab_logical gap is recorded as BLOCKED · #bugfix #macos #vision

Pillow 12.3.0's darwin branch returns a Retina capture at twice its size in points, and scales a `bbox` capture back only when `scale_down=True` is passed (Pillow docs: "screen captures will be at 2x if on a Retina screen"). `grab_screen_region` (U-20260926-06) handed macOS regions to `pil_screenshot`, which does not pass it, so on a Retina Mac a colour blob found in a region was placed twice as far from the region's corner as it is. The mouse (Quartz) takes points.

- `grab_screen_region` on macOS now calls the platform grabber with `bbox` and `scale_down=True`. `screencapture -R` takes global point coordinates, negative ones included, so every display is still reached. Windows (`grab_logical`), X11 / Wayland and the whole-screen capture are unchanged.
- `test_region_capture_monitors.py`: a fake Retina grabber that doubles unless `scale_down` is passed checks the macOS path; the X11 / Wayland case keeps `pil_screenshot`. Not measured on a Retina Mac: the GitHub macOS runner is a 1x virtual display.
- **Recorded in `Progress.md`** (BLOCKED, Jeffrey_RPA's live capture path): `grab_logical`, which the matchers and OCR use, has the same 2x problem on macOS and captures only the main display, since Pillow ignores `all_screens` there.
- **Files**: `utils/cv2_utils/region_capture.py`, its test, `Progress.md`, `architecture_explore.md` (line counts).

## U-20260926-10 · 2026-09-26 · Element geometry is read in any shape and equal zero-area boxes match; interactive roles are known in UIA, AT-SPI, macOS AX and ARIA spelling and Tab skips disabled controls; quality and motion are measured on 8-bit frames; minimized windows have no client point; a NaN focus wait is refused · #bugfix #audit #accessibility #vision

A read-only audit agent found these, each reproduced with its probe and re-checked here. `test_geometry_roles_frames_audit.py` (new, 32) fails 25/32 on the old code; the other seven guard static roles that must stay non-interactive. The 2,542 existing tests for these modules pass.

- **Geometry**: `element_parse` read only `x` / `y` / `width` / `height`, so `fuse_elements` on `AC_a11y_list` output (`bounds`) raised `KeyError`. So did `observation_index` and `classify_effect` on an element with a point but no size, which is the Script Builder placeholder for `AC_classify_effect`. Geometry now goes through `element_box`, missing keys read as 0, and `observation` / `action_effect` share `element_parse._center`.
- **Equal zero-area boxes**: the IoU of a 0x0 box with itself was 0. Hidden-tab controls report (0, 0, 0, 0), so an unchanged frame read as one element removed and one added. Equal boxes now have IoU 1.
- **Roles**: `is_interactive_role` knew only UIA names, and `observation` kept a separate lower-case set without `RadioButton`, `Hyperlink`, `TabItem`, `TreeItem`, `SplitButton` or the raw `ControlType_*` form. One set now serves both, keyed case- and space-insensitively and covering UIA, AT-SPI (`push button`, what the Linux backend reports), macOS AX (`AXTextField`) and ARIA. On Linux and macOS, `tab_order` and `audit_focus_order` returned nothing.
- **Disabled controls**: `tab_order` listed them, although `Tab` skips them.
- **Action effects**: `{"x": None}` (a cua action with no coordinate) raised `TypeError`; it now means no target point. `radius` was a square reaching 1.41 x at the corners; it is a circle now. The observation viewport's right and bottom edges are now exclusive.
- **8-bit frames**: `image_quality` read a uint16 mid-gray as brightness 32028 (`too_bright`), a PIL `I;16` image as saturated white, and raised `cv2.error` on a float frame. `motion_regions` raised on uint16 in `connectedComponents`, never saw motion in 0..1 float frames, and counted a 0.4-level 16-bit change as motion. Both measure 8-bit frames through `preprocess._eight_bit`, which is shared now with `annotate` (added in U-20260926-07), and contain OpenCV errors.
- **Minimized windows**: `get_client_rect` / `client_point` answered (-32000, -32000, 0, 0) and an off-screen point for a minimized window. They answer `None`, as `window_capture` does, and `ClientToScreen` failures are checked.
- **Focus waits**: `wait_for_focus_change(timeout=nan)` blocked forever with the UIA handler still registered, and `inf` raised `OverflowError`. NaN is a `ValueError`, a negative timeout looks once and `inf` waits for good.
- **Separate batch**: the executor adapters' `bool()` on string flags (`"false"` turned on `ignore_case`) is fixed across all adapters in the next change.
- **Docs**: the v152, v167 and v184 feature docs (Eng/Zh).
- **Files**: `utils/{element_parse,observation,action_effect,focus_order,image_quality,motion_regions,window_geometry,ax_events,preprocess,annotate}/*.py`, `utils/accessibility/backends/windows_backend.py`, the new test, the feature docs and `architecture_explore.md` (line counts).
## U-20260926-11 · 2026-09-26 · Every executor adapter and flow command reads on / off flags by their spelling through one as_bool, so "false" means off (it turned on ignore_case, pasted over the clipboard, and re-raised) · #bugfix #executor

A value from a JSON action file, the CLI, the socket server or an MCP call can arrive as the string `"false"`, which `bool()` reads as true. The executor already had `_as_bool` for this, but only a few adapters used it. 85 adapters in `action_executor.py` still called `bool()` on their own flag parameters: `ignore_case`, `present`, `raise_on_fail`, `capture_on_fail`, `regex`, `exists`, `case_sensitive`, `dry_run`, `paste` and so on. `flow_control` (`reraise`) and `flow_data_commands` (`raise_on_fail`) did the same. The audit agents reproduced `AC_find_control_text {"ignore_case": "false"}` reaching the backend as `True`, and `AC_set_field_text {"paste": "false"}` pasting and overwriting the user's clipboard.

- `utils/executor/flags.py` (new) holds `as_bool`: a string is on only as `"true"` / `"yes"` / `"on"` / `"1"`, and anything else is `bool()`. `action_executor` imports it as `_as_bool`, and every such call site in the three executor modules uses it. The one exception is `AC_eval_check`'s `truthy` operator, which tests data, not a flag.
- `test_executor_flags.py` (new, 21): the spellings; an AST check that no function in the three modules calls `bool()` on one of its own parameters or on `args.get(...)`; and the three reproductions (`ignore_case`, `paste`, `reraise`). 6/21 fail on the old executor (the other 15 test `as_bool` itself). The flow-control suite (90) passes.
- **Docs**: the Keywords & Executor reference (Eng/Zh) states how flags are read.
- **Files**: `utils/executor/{flags (new),action_executor,flow_control,flow_data_commands}.py`, the new test, the reference doc and `architecture_explore.md` (executor file table, line counts).
## U-20260926-12 · 2026-09-26 · AC_grid_cell reads locate_all_image's [left, top, right, bottom] boxes with box_format="ltrb"; macOS field clearing uses Backspace; dataset diffs refuse a missing key column and agree cell by cell; runs given as one name and pairs that never failed together no longer cluster; unknown critic effects, bad TOTP times and negative windows are refused; deep_merge shares nothing with its inputs · #bugfix #audit #data

A read-only audit agent found these, each reproduced with its probe and re-checked here. `test_grid_dataset_totp_audit.py` (new, 16) fails 14/16 on the old code; the two that pass guard the xywh default and a `-1` TOTP time, which was already refused. The 376 existing tests for these modules pass.

- **Grid cells**: the docs say `AC_locate_all_image` output feeds `AC_grid_cell`, but `find_image_multi` answers `[left, top, right, bottom]` and every box was read as `[x, y, w, h]`: the (1, 1) centre came out as (320, 460) instead of (220, 310). `cluster_grid`, `locate_cell`, `AC_grid_cell` and `ac_grid_cell` take `box_format` (`xywh` by default, or `ltrb`). Dicts and OCR match objects are read through `element_box`; they raised `KeyError` / `TypeError`. There is a new Script Builder field; the MCP tool count is unchanged.
- **Field entry on macOS**: the select-all clear pressed `"delete"`, which the macOS key table does not have. `set_field_text(..., modifier="command")` selected everything, raised, and typed nothing. On macOS it presses `"backspace"`.
- **Dataset diffs**:
  - A misspelt or empty key keyed every row as `None`, so `diff_rows` compared two arbitrary rows and reported one change. A row without a key column, or an empty key, now raises `ValueError`.
  - `cell_changes` compared with `.get()`, so a new `None` column changed the row but listed no cell, and NaN cells were listed as changes. Both functions now share `_same`: NaN equals NaN, and a column present on only one side is a change.
- **Co-failure clusters**: a run given as one test name was split into its letters. At `threshold=0`, tests that never failed together were paired. Both are fixed.
- **Critic scores**: an unknown effect (`"error"`, or a typo such as `"noop"`) scored `outcome: True`. It raises `ValueError`.
- **TOTP**: `at=nan` raised `ValueError` and `inf` / `1e21` raised `OverflowError`, outside the framework's exception family, and `window=-1` rejected even the current code. All are `TOTPError` now.
- **Config merge**: `deep_merge` shared `base`'s untouched sub-dicts with the result, so editing the merged config edited the defaults. The result is a deep copy.
- **Paragraphs**: a line with text `None` added the word "None".
- **Handled in U-20260926-11**: the agent's `AC_set_field_text {"paste": "false"}` finding is the `bool()` flag problem fixed across all adapters there.
- **Docs**: the v81, v83, v117, v125, v177 and v193 feature docs (Eng/Zh).
- **Files**: `utils/{grid_locator,field_entry,dataset_diff,flake_cluster,critic_features,layered_config,text_blocks}/*.py`, `utils/remote_desktop/totp.py`, `utils/executor/action_executor.py` (`AC_grid_cell`), `utils/mcp_server/tools/{_factories,_handlers_executor_bridge}.py`, `gui/script_builder/command_schema.py`, the new test, the feature docs and `architecture_explore.md` (line counts).
## U-20260926-13 · 2026-09-26 · Repair commands without a db share one store per process; approval artifacts store numbers and lists as JSON; two quarantine stores on one file keep each other's names; renewal intervals must be positive and renewals never overlap; NumPy bboxes work in force mode; failing plugin imports are reported; timelines refuse NaN and negative durations · #bugfix #audit #infra

A read-only audit agent found these, each reproduced with its probe and re-checked here. `test_repair_quarantine_renewal_audit.py` (new, 19) fails 17/19 on the old code; the two that pass guard text and bytes artifacts. The 348 existing tests for these modules pass. `test_json_store_audit.py` now watches `json_store`'s writer for the quarantine store, which writes through `SharedJsonDict`.

- **Locator repair**: every `AC_repair_*` command and MCP tool built `RepairStore(db)`, so without a `db` each call had a fresh in-memory store. `AC_repair_resolved` / `pending` / `approve` never saw what `AC_repair_record` stored. `repair_store(db)` shares one store per process, as `approval_gate` does since its own fix.
  - A hand-edited file raised `KeyError` or `AttributeError`; rows that are not objects, or lack keys, are now read around.
  - `confidence="0.95"` raised `TypeError`; `record` converts both numbers with `float()`.
- **Approval artifacts**: `bytes(3)` is three NUL bytes and `bytes([104, 105])` is `b"hi"`. So `AC_verify_artifact` stored a number or list as the wrong artifact and raised on a dict. Bytes stay as they are, text is UTF-8 and any other value is sorted, indented JSON. `pending_artifacts` lists a name once when it has two received extensions.
- **Quarantine**: the store read its file once and wrote its whole copy back on every change, so a long-running runner holding the default store wiped names the CLI or GUI had added. It is now built on `SharedJsonDict`: lock, re-read, change, atomic write. Every read is fresh.
- **ACME renewal**:
  - `check_interval_s` of 0, negative or NaN made `stop.wait()` return at once, so a failing renewal ran certbot ~20,000 times a second, and Let's Encrypt rate-limits failed validations. The interval must now be finite and positive.
  - A restart during a renewal ran a second one alongside it. Renewals now take a lock and re-check `renewal_due` after one in progress finishes. The fresh-run restart convention that `test_background_restart_keeps_old_run_stopped` pins is kept.
- **Act modes**: force mode tested `not bbox`, which raises for a NumPy array the other modes accept. The actionability centre was `np.int64`, which `json.dumps` rejects. Both are plain ints now.
- **Package manager**: a plugin whose module body raises (`RuntimeError("missing config")`), or `check_package("__main__")`, escaped; they are logged and reported as not loaded.
- **Step timeline**: NaN or inf put NaN into `busy` / `parallelism` / `pct` (not JSON), and a negative duration gave -50 %. Both now raise `ValueError` naming the step.
- **Not changed**: `ensure_toggle` against a reader that lags one read toggles twice. The agent flagged it as a possible design issue, not a reproduced bug.
- **Docs**: the v3, v35, v54 and v194 feature docs (Eng/Zh).
- **Files**: `utils/{locator_repair/{locator_repair,__init__},approval/approval_test,quarantine/store,tls_acme/renewal,act_modes/act_modes,actionability/actionability,package_manager/package_manager_class,step_timeline/step_timeline,executor/action_executor}.py`, `utils/mcp_server/tools/_handlers_locators.py`, two tests, the feature docs and `architecture_explore.md` (line counts).

## U-20260926-14 · 2026-09-26 · The offscreen screen-probe tests pin sys.platform, so macOS runners check the scaled case as other platforms do instead of failing it · #tests #ci #macos

`pytest-headless` on `macos-14` (3.10 and 3.14) failed on two tests since 734a418e made `capture_ratio` 1.0 on macOS, where captures and the pointer take points. `test_rd_annotation_screens[scaled]` expected (8.0, 16.0) and got (10.0, 20.0); `test_region_selector_screens[scaled]` expected (2045, -39, 251, 126) and got (2020, -64, 201, 101). The code was right on macOS; the tests' plain cases read the runner's real `darwin`. Both probes now pin `sys.platform`: `linux` for the plain cases and `darwin` for the `-mac` ones. The annotation test gains a `scaled-mac` case, (10.0, 20.0). Found by reading the failed jobs of Code Quality runs on 7ee18e5a and a515e613; the other jobs and platforms passed.

- **Files**: `test/unit_test/headless/{test_rd_annotation_screens,test_region_selector_screens}.py`.

## U-20260926-15 · 2026-09-26 · Sub-pixel and scale matching take 16-bit and float images and centre on the right pixel; one unscorable scale no longer aborts a sweep; rbac saves before it changes and hands out frozen copies; failure bundles mask secrets in nested mappings, tuples and reprs; malformed rubrics are refused; an aborted drag releases where it stopped and a click without a point clicks in place · #bugfix #security #audit #vision

A read-only audit agent found these, each reproduced with its probe and re-checked here. `test_match_rbac_bundle_audit.py` (new, 13) fails 13/13 on the old code; the 295 existing tests for these modules pass. `visual_match` is on Jeffrey_RPA's live path and is not edited: the fixes sit in its callers.

- **Matching 16-bit and float images**: `match_subpixel` and `scale_sweep` / `detect_scale` handed them to OpenCV as they were. uint16, float64 and mixed inputs raised `cv2.error` past the executor; a 0..1 float template read as flat (std 0.28 in 8-bit units). Both now match through `preprocess._eight_bit` and contain OpenCV errors.
- **Scale sweeps**:
  - A scale that cannot be scored is skipped: `[1e6]` failed to allocate and aborted the run, and a checkerboard blurred flat at 0.5.
  - A NumPy array of scales raised on its truth value.
  - Non-finite or non-positive scales raise `ValueError`.
- **Sub-pixel centre**: `cx` was `x + w / 2 + offset`, half a pixel right of the centre column for an odd width (42.507 for an exact 25 px match at 30). It is `x + (w - 1) / 2 + offset`, the centre in pixel indices.
- **rbac** (security):
  - The store changed users in memory and then saved. A user added to an unreadable store could sign in as admin despite the refusal. A save failing with `OSError` left the new role in memory, and a failed `rotate_token` locked the user out.
  - Changes are now saved first and swapped in only on success, and `OSError` becomes `UserAuthError`.
  - `UserRecord` is frozen and `get` / `list_users` / `authenticate` hand out copies. A caller setting `record.role = "admin"` had changed the store.
- **Failure bundles** (security): redaction walks dicts and lists only. A `mappingproxy` of headers, a tuple `AC_secret_set` action and objects written through `default=repr` (`Conn(password=...)`) reached `manifest.json` in clear text. Values are converted to plain dicts and lists at every depth first, and the `repr` fallback goes through `redact_secret_text`.
- **Trajectory rubrics**: a list rubric or a misspelt key (`"forbiden_actions"`) made no checks, and no checks passed with score 1.0. A rubric that is not an object, or has unknown keys, raises `ValueError`.
- **Gestures**:
  - `drag_path` released at the last waypoint when a move failed, completing the drop the drag never reached. It now releases where the pointer stopped.
  - `run_sequence`'s `click` without a point clicked (0, 0), the top-left hot corner. It clicks in place; a `move` without a point raises.
  - A negative `wait` is 0 (it raised in `time.sleep`), and a non-finite `wait` or replay gap raises `ValueError`; `sleep(inf)` never returned.
- **Docs**: the v24, v36, v116 and v171 feature docs (Eng/Zh).
- **Files**: `utils/{scale_detect,subpixel_match,trajectory_eval,rbac/users,failure_bundle/bundle,mouse_path,input_macro/input_macro}.py`, the new test, the feature docs and `architecture_explore.md` (line counts).

## U-20260926-16 · 2026-09-26 · The headless suite records any input that reaches the real mouse or keyboard backend and fails that test; the recorder replay test no longer moves the real cursor · #tests #safety

A read-only audit agent noticed that `test_recorder_base.py::test_replaying_a_recording_drives_the_matching_input_calls` faked `press_mouse` / `release_mouse` but not `set_mouse_position`. The replay moves to each event first, so every suite run moved the real cursor to (1, 2), on a desktop where Jeffrey_RPA may be typing.

- **The test** now fakes the move as well.
- **A guard for every test**: `test/unit_test/headless/conftest.py` gains an autouse `refuse_real_input` fixture. It swaps the platform mouse and keyboard backends for a recorder, both in `platform_wrapper` and in the names `auto_control_mouse` / `auto_control_keyboard` bound at import. An input call that reaches the recorder does nothing, and the test fails at teardown naming the call. A test that installs its own backend fake replaces the recorder and is unaffected.
- The guarded calls are `mouse_event`, `set_position`, `press_mouse`, `release_mouse`, `click_mouse`, `scroll`, the `send_*_event_to_window` posts, `press_key` / `release_key` and the Unicode key calls.
- The whole headless suite (10,186 tests) passes with the guard in place: no other test reached the real backend.
- **Files**: `test/unit_test/headless/{conftest,test_recorder_base}.py`.
## U-20260926-17 · 2026-09-26 · AC_rate_limit takes a changed rate for a reused name; the app-idle wait keeps its deadline; retry jitter and caps are validated; asset types refuse values they cannot hold; memory stores can be read while written; hand-written skills keep their tags and self-calling skills are bounded; blank locators are refused; the MCP saga reports rollback errors · #bugfix #audit #infra

A read-only audit agent found these, each reproduced with its probe and re-checked here. `test_rate_assets_skills_audit.py` (new, 20) fails 19/20 on the old code; the one that passes guards the values asset types keep. The 504 existing tests for these modules pass.

- **Rate limits**: the executor adapter kept its own `setdefault` table, so a reused name ignored a new rate or capacity. Only the MCP handler had been fixed. Both now use `rate_limit.named_bucket(name, rate, capacity)`, one lock-guarded registry that rebuilds a bucket when either value changes.
  - Library edges: `TokenBucket.acquire(timeout=nan)` never timed out, `throttle(nan)` never throttled, and `SlidingWindowLimiter(0.5, ...)` became a limit of 0 while `inf` raised `OverflowError`. All three now raise `AutoControlException`.
- **App idle**: the poll slept `interval_s` whatever the deadline. A 3600 s interval made a 1 s wait take an hour, NaN or a negative interval raised in `time.sleep`, and 0 spun (147,150 cursor probes in 0.3 s). It clamps like `ime_state` and never sleeps past the deadline.
- **Retry budgets**:
  - An unknown jitter (`"nnone"`, `"fast"`) became full jitter. Jitter is case-folded and validated.
  - An infinite `max_delay_s` put `inf` into executor and MCP results; base and cap must be finite and at least 0.
  - With base 0 an overflowing attempt returned the cap instead of 0.
  - `AC_plan_retry_delays` caps `attempts` at 10,000.
  - A JSON `null` jitter takes the adapter default.
- **Assets**: an `int` asset stored 3.7 as 3 and `True` as 1, and `inf` raised `OverflowError` rather than the promised `ValueError`. A `bool` asset read `"enabled"` as False. Ints must be whole numbers, and bools a known spelling.
- **Memory stores**: `SharedJsonDict.read()` returned the live dict outside the lock. `AssetStore.list()` on the process store raised `dictionary changed size during iteration` in 299 of 300 reads while another thread wrote. Reads now return a deep copy taken under the lock.
- **Skills**:
  - Hand-written tags given as one string were split into letters (`search("login")` missed, `search("l o")` hit). A list of numbers raised for the whole library. An entry that is not an object made every read raise; it is now skipped.
  - A skill that runs itself recursed to `RecursionError`, recorded as an ordinary failure. Skills are now bounded at 50 like macros, and the depth error reaches the outermost `AC_skill_run`.
  - Nested skill runs use the executor that is running them.
- **Element repository**: `save(name="")` passed the at-least-one-filter check and matched the first unnamed element, the desktop. Blank values are refused, and non-object entries are skipped.
- **Sagas**: MCP `ac_run_saga` dropped `compensation_errors`; it now delegates to the executor adapter. `list(action)` broke the `{"auto_control": [...]}` form, and a step that is not an object raised `AttributeError`; it now raises `ValueError` naming the step.
- **Docs**: the v12, v13, v48, v62 and v209 feature docs (Eng/Zh).
- **Files**: `utils/{rate_limit/{rate_limit,__init__},app_idle/app_idle,retry_budget/retry_budget,assets/assets,json_store/json_store,skill_library/skill_library,element_repository/element_repository,saga/saga,executor/action_executor}.py`, `utils/mcp_server/tools/_handlers_operations.py`, the new test, the feature docs and `architecture_explore.md` (line counts).

## U-20260926-19 · 2026-09-26 · The WebRTC host, transport and Opus track take the running event loop with asyncio.get_running_loop() instead of the deprecated-outside-a-loop get_event_loop() · #maintenance #remote-desktop #python

A sweep for stdlib APIs Python 3.12-3.14 deprecates found five `asyncio.get_event_loop()` calls under `utils/remote_desktop/`. Four run inside the loop: `WebRTCDesktopHost._async_accept_answer`, `ScreenVideoTrack.recv`, `wait_for_ice_gathering` (all coroutines) and `OpusMicAudioTrack.__init__`, which the host and viewer build inside their coroutines. There `get_running_loop()` is the documented call, and it raises at once if ever used off the loop. `get_event_loop()` either warns, raises (3.14, no current loop) or hands back a loop that never runs, so the track's `call_soon_threadsafe` would go nowhere. The fifth, `webrtc_host_auth._schedule_close_after_fail`, runs on the loop in production, but its tests call it synchronously through a fake bridge, so it keeps `get_event_loop()`. The 826 WebRTC and remote-desktop tests pass.

- **Files**: `utils/remote_desktop/{webrtc_audio,webrtc_host,webrtc_transport}.py`.
## U-20260926-20 · 2026-09-26 · Every MCP tools/call is answered whatever the tool raises; plain HTTP POSTs answer in their body on a concurrent server; ac_egress_reset and ac_approve_artifact need confirmation; an unwritable audit log no longer fails a tool that ran; the JSON document tools take root arrays; malformed JSON-RPC envelopes are -32600 and an id of null is a request · #bugfix #security #mcp #audit

A read-only audit agent found these, each reproduced with its probe and re-checked here. `test_mcp_envelope_containment_audit.py` (new, 20) and two new safety pins in `test_mcp_tool_safety_audit.py` fail 19 on the old code. The 373 existing MCP and data-quality tests pass.

- **Unanswered calls**: `tools/call` caught a fixed list of error types. `re.PatternError` (a bad regex given to `ac_validate_rows` or `ac_extract_fields`), `ET.ParseError`, `cv2.error` and a plugin's own exceptions escaped it. The stdio worker died and the call never got a reply; over HTTP the connection closed without one. A tool's failure of any type is now `isError: true`, and `_TOOL_INVOKE_ERRORS` is gone. `data_quality` also reports a bad regex as `ValueError`.
- **Plain POSTs on a concurrent server**: the two plain-POST paths did not pass `concurrent_tools=False` as the SSE paths do. With `MCPServer(concurrent_tools=True)` a `tools/call` went to a worker, and the POST came back `202` with an empty body.
- **Confirmation gate** (security): `ac_egress_reset` does what `ac_egress_allow({})` does but was annotated side-effect-only. With confirmation on, the operator's denial of `ac_egress_allow` could be undone without a prompt. `ac_approve_artifact` overwrites the approved baseline like `ac_approval_approve`. Both are destructive now and pinned by the safety audit.
- **Audit log**: `AuditLogger.record` runs after the tool. An `OSError` there (a missing directory) turned a click that had happened into `-32603 Internal error`, inviting a retry, and put the log's absolute path in the reply. It is now logged instead.
- **Root arrays**: `ac_json_query`, `ac_json_extract`, `ac_validate_json`, `ac_match_json`, `ac_diff_json`, `ac_resolve_pointer` and `ac_apply_json_patch` declared their documents as objects. The server refused a root array that the handler and the `AC_*` command both accept. They now take an object or an array.
- **JSON-RPC envelopes**:
  - `{"id": null, "method": "ping"}` was treated as a notification, so the client waited forever. A notification is now only a message without an `id` member.
  - A request without `method` was `-32601 "Method not found: None"`, and a list `method` was `-32603`.
  - Object, array and boolean ids were echoed back, and a missing or wrong `jsonrpc` was served.
  - All of these are now `-32600`. Replies from the client to the server's own requests are still dropped silently. `handle_line` was split to stay under the complexity limit.
- **Docs**: the MCP server doc (Eng/Zh) states how tool failures, envelopes and the audit log behave.
- **Files**: `utils/mcp_server/{server,_protocol,http_transport,audit}.py`, `utils/mcp_server/tools/_factories.py`, `utils/data_quality/data_quality.py`, the new test, `test_mcp_tool_safety_audit.py`, `test_mcp_and_devices_audit.py` (no longer pins the removed tuple), the MCP doc and `architecture_explore.md` (line counts).
